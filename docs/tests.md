# Test Cases

This document catalogs every test case in the PiSSM project. Each test is linked to a specific functional or non-functional requirement from the SRS. The requirement reference sections below provide a quick lookup so test tables can be read without switching to the SRS.

## Functional Requirements Reference

### Node Management

- **FR-NM-01:** Each worker node shall broadcast a heartbeat to the orchestrator at a configurable interval (default: 2 seconds).
- **FR-NM-02:** The orchestrator shall maintain a live node registry tracking each node's identity, available RAM, and last heartbeat timestamp.
- **FR-NM-03:** A node that misses three consecutive heartbeats shall be marked as unavailable and excluded from dispatch decisions.
- **FR-NM-04:** The orchestrator shall support listing all nodes with their current status.
- **FR-NM-05:** Nodes shall automatically rejoin the cluster upon restart without manual intervention.

### Model Registry

- **FR-MR-01:** The system shall maintain a registry of submitted models, storing the manifest, checkpoint path, and compilation status.
- **FR-MR-02:** A user shall be able to submit a model via `compile <manifest.yaml>` (TUI) or the WebUI upload form.
- **FR-MR-03:** The system shall validate the manifest on submission, reporting clear errors for missing fields or unsupported architecture values.
- **FR-MR-04:** A user shall be able to list registered models via `ls models` (TUI) or the WebUI model list.
- **FR-MR-05:** A user shall be able to delete a registered model.

### Dispatch Engine

- **FR-DE-01:** On receiving a run request, the orchestrator shall query available RAM across all live nodes.
- **FR-DE-02:** The dispatch engine shall apply dispatch rules in order: single node, layer parallel, quantized fallback, rejection.
- **FR-DE-03:** The dispatch decision and resulting topology shall be recorded and viewable by the user.
- **FR-DE-04:** The topology shall be inspectable and manually overridable via `vim topology.yaml` (TUI) or the topology editor (WebUI).

### Inference Execution

- **FR-IE-01:** The system shall accept user input appropriate to the model's input_type (text prompt, numeric array, or audio file path).
- **FR-IE-02:** For text input, the system shall run the associated tokenizer before passing data to the first worker shard.
- **FR-IE-03:** For timeseries or audio input, the system shall normalize and chunk the input to the model's configured sequence length.
- **FR-IE-04:** Pipeline execution shall pass activations between worker nodes via gRPC.
- **FR-IE-05:** The final node shall return the output to the orchestrator, which returns it to the user interface.
- **FR-IE-06:** Inference latency (end-to-end) and per-node execution time shall be recorded for every request.

### Fault Tolerance

- **FR-FT-01:** If a worker node becomes unavailable during an active inference request, the orchestrator shall detect the failure and abort the current request with an informative error.
- **FR-FT-02:** The orchestrator shall immediately attempt to re-dispatch the same request to remaining live nodes using the dispatch rules.
- **FR-FT-03:** The node registry shall be updated immediately upon failure detection.
- **FR-FT-04:** Recovery of a failed node shall be automatic upon reconnection.

### TUI

- **FR-TUI-01:** The TUI shall launch as a standalone terminal application and connect to the orchestrator's HTTP API.
- **FR-TUI-02:** The TUI shall support commands: `listn`, `ls models`, `compile`, `run`, `vim topology.yaml`, `status`, `logs`.
- **FR-TUI-03:** The TUI shall display a persistent status bar showing live node count and cluster RAM utilization.
- **FR-TUI-04:** The TUI shall display inference output and timing results inline after a `run` command completes.

### WebUI

- **FR-WUI-01:** The WebUI shall be served by the orchestrator and accessible at `http://<orchestrator-ip>:8080`.
- **FR-WUI-02:** The WebUI shall provide a dashboard showing node status, cluster RAM, and recent inference history.
- **FR-WUI-03:** The WebUI shall allow model submission via a file upload form accepting a manifest YAML and checkpoint file.
- **FR-WUI-04:** The WebUI shall provide an inference panel where the user selects a registered model, enters input, and views output.
- **FR-WUI-05:** The WebUI shall display the active topology visually.
- **FR-WUI-06:** The WebUI shall display per-request benchmark data (latency, throughput, memory usage per node).

### Benchmarking

- **FR-BM-01:** The system shall record for every inference request: total latency, per-node execution time, activation transfer time, and peak memory usage per node.
- **FR-BM-02:** The system shall expose a benchmark mode where the same input is run N times and aggregate statistics are reported.
- **FR-BM-03:** Benchmark results shall be exportable as CSV.

## Non-Functional Requirements Reference

- **NFR-01 (Performance):** End-to-end inference latency for Mamba-130M on a single node shall be under 5 seconds for a 512-token input at steady state.
- **NFR-02 (Reliability):** The system shall handle single-node failure without crashing the orchestrator or other workers.
- **NFR-03 (Portability):** All software shall run on Raspberry Pi OS (64-bit, Debian Bookworm base) without modification.
- **NFR-04 (Usability):** A user unfamiliar with the system shall be able to submit and run a model within 10 minutes using the WebUI, given a valid manifest and checkpoint.
- **NFR-05 (Observability):** All inter-node gRPC calls shall be logged with timestamps.

---

## Unit Tests

### NodeRegistry (`tests/unit/test_node_registry.py`)

Component under test: `orchestrator.node_registry.NodeRegistry` - a thread-safe in-memory store of worker node state. All unit tests use an injectable mock clock to control time deterministically.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-NR-01 | FR-NM-02 | TestNodeRegistration | Register new node | Verify that registering a node stores all fields correctly and sets the initial status to available. | Empty registry with mock clock at t=100.0. | node_id="node-1", ip="192.168.1.10", available_ram=3800, total_ram=4096, cpu_count=4, arch="aarch64", os_name="Linux", os_version="6.6.31+rpt-rpi-2712", inference_port=50052. | 1. Create registry with mock clock at t=100.0. 2. Call `update_node` with test data. 3. Call `get_node("node-1")`. | All fields match input, status="available", last_heartbeat=100.0, first_seen=100.0, inference_port=50052. |
| TC-NR-02 | FR-NM-02 | TestNodeRegistration | Update existing node | Verify that a second heartbeat updates RAM and timestamp but preserves first_seen. | Registry with mock clock at t=100.0, one registered node. | Same node_id, first call available_ram=3800, second call available_ram=3500 after 2s advance. | 1. Register node-1 with available_ram=3800 at t=100.0. 2. Advance clock by 2.0s. 3. Register node-1 again with available_ram=3500. 4. Call `get_node("node-1")`. | available_ram_mb=3500, last_heartbeat=102.0, first_seen=100.0. |
| TC-NR-03 | FR-NM-02 | TestNodeRegistration | Lookup unknown node | Verify that looking up an unregistered node returns None. | Empty registry. | node_id="nonexistent". | 1. Create empty registry. 2. Call `get_node("nonexistent")`. | Returns None. |
| TC-NL-01 | FR-NM-04 | TestNodeListing | List nodes on empty registry | Verify that listing on an empty registry returns an empty list. | Empty registry. | None. | 1. Create empty registry. 2. Call `list_nodes()`. | Returns []. |
| TC-NL-02 | FR-NM-04 | TestNodeListing | List all registered nodes | Verify that all registered nodes are returned with no filter. | Empty registry. | Three nodes: node-1, node-2, node-3. | 1. Register node-1, node-2, node-3. 2. Call `list_nodes()`. | Returns 3 items with node_ids {"node-1", "node-2", "node-3"}. |
| TC-NL-03 | FR-NM-04 | TestNodeListing | Filter nodes by status | Verify that filtering by status returns only matching nodes after one is reaped. | Registry with heartbeat_interval=1.0s, missed_threshold=3, mock clock at t=0.0. | Two nodes: node-1, node-2. | 1. Register both at t=0. 2. Advance clock by 4.0s. 3. Register node-1 again. 4. Call `reap_stale_nodes()`. 5. Call `list_nodes` with each filter. | Available list: [node-1]. Unavailable list: [node-2]. |
| TC-FD-01 | FR-NM-03 | TestFailureDetection | Reap stale node | Verify that a node past the timeout is marked unavailable and returned by the reaper. | Registry with heartbeat_interval=2.0s, missed_threshold=3 (timeout=6.0s), mock clock at t=0.0. | One node: node-1. | 1. Register node-1 at t=0. 2. Advance clock by 7.0s. 3. Call `reap_stale_nodes()`. 4. Call `get_node("node-1")`. | Reaper returns ["node-1"]. Node status is "unavailable". |
| TC-FD-02 | FR-NM-03 | TestFailureDetection | Do not reap fresh nodes | Verify that nodes within the timeout window are not reaped. | Registry with heartbeat_interval=2.0s, missed_threshold=3 (timeout=6.0s), mock clock at t=0.0. | One node: node-1. | 1. Register node-1 at t=0. 2. Advance clock by 1.0s. 3. Call `reap_stale_nodes()`. 4. Call `get_node("node-1")`. | Reaper returns []. Node status is "available". |
| TC-FD-03 | FR-NM-03 | TestFailureDetection | Do not reap already unavailable | Verify that a second reap does not report the same node again. | Registry with heartbeat_interval=2.0s, missed_threshold=3 (timeout=6.0s), mock clock at t=0.0. | One node: node-1. | 1. Register node-1 at t=0. 2. Advance clock by 7.0s. 3. Call `reap_stale_nodes()`. 4. Call `reap_stale_nodes()` again. | First reap returns ["node-1"]. Second reap returns []. |
| TC-FD-04 | FR-NM-03 | TestFailureDetection | Timeout calculation | Verify that timeout_s equals heartbeat_interval_s * missed_threshold. | None. | heartbeat_interval_s=2.0, missed_threshold=3. | 1. Create registry with given parameters. 2. Read `timeout_s`. | timeout_s == 6.0. |
| TC-AR-01 | FR-NM-05 | TestAutoRejoin | Rejoin after unavailable | Verify that a previously unavailable node is restored to available on a new heartbeat. | Registry with heartbeat_interval=2.0s, missed_threshold=3 (timeout=6.0s), mock clock at t=0.0. | One node: node-1. | 1. Register at t=0. 2. Advance by 7.0s. 3. Reap. 4. Advance by 1.0s. 5. Register again. 6. Call `get_node("node-1")`. | status="available", last_heartbeat=8.0, first_seen=0.0. |
| TC-TS-01 | NFR-02 | TestThreadSafety | Concurrent updates | Verify that 10 threads updating different nodes simultaneously causes no corruption. | Empty registry with default clock. | 10 threads, each updating a unique node_id 100 times. | 1. Create empty registry. 2. Spawn 10 threads. 3. Join all. 4. Check for exceptions. 5. Call `list_nodes()`. | No exceptions. Registry contains exactly 10 nodes. |

### System Info (`tests/unit/test_system_info.py`)

Component under test: `worker.system_info` - functions that gather node identity and hardware state for heartbeat payloads. These wrap `socket`, `platform`, and `psutil` calls.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-SI-01 | FR-NM-01 | TestNodeIdentity | Node ID is non-empty string | Verify that get_node_id returns a non-empty string. | None. | None. | 1. Call `get_node_id()`. | Returns a non-empty string. |
| TC-SI-02 | FR-NM-01 | TestNodeIdentity | IP address is valid IPv4 | Verify that get_ip_address returns a dotted-quad IPv4 string. | Network connectivity. | None. | 1. Call `get_ip_address()`. 2. Split by ".". 3. Validate each octet. | Four octets, each 0-255. |
| TC-SI-10 | FR-NM-01 | TestIpAddressDetection | Override is returned as-is | Verify an explicit override skips detection entirely. | None. | override="10.0.0.5". | 1. Call `get_ip_address("10.0.0.5")`. | Returns "10.0.0.5". |
| TC-SI-11 | FR-NM-01 | TestIpAddressDetection | Prefers Ethernet over Wi-Fi | Verify a wired interface's address wins even when a Wi-Fi interface is also up - the actual bug: connecting out to detect a default route often picks Wi-Fi even when Ethernet is the intended cluster link. | `psutil.net_if_addrs` mocked with lo, wlan0, and eth0 entries. | eth0="192.168.1.99", wlan0="192.168.1.50". | 1. Call `get_ip_address()`. | Returns "192.168.1.99" (the eth0 address), not the Wi-Fi one. |
| TC-SI-12 | FR-NM-01 | TestIpAddressDetection | Falls back with no wired interface | Verify detection falls back to the connect-out trick when no eth*/en* interface is present, rather than returning nothing. | `psutil.net_if_addrs` mocked with only lo and wlan0. | wlan0="192.168.1.50". | 1. Call `get_ip_address()`. | Returns a valid dotted-quad IPv4 string. |
| TC-SI-03 | FR-NM-02 | TestMemoryInfo | Available RAM is positive int | Verify that get_available_ram_mb returns a positive integer. | None. | None. | 1. Call `get_available_ram_mb()`. | Returns int > 0. |
| TC-SI-04 | FR-NM-02 | TestMemoryInfo | Total RAM is positive int | Verify that get_total_ram_mb returns a positive integer. | None. | None. | 1. Call `get_total_ram_mb()`. | Returns int > 0. |
| TC-SI-05 | FR-NM-02 | TestMemoryInfo | Available RAM does not exceed total | Verify that available RAM is less than or equal to total RAM. | None. | None. | 1. Call both functions. 2. Compare. | available <= total. |
| TC-SI-06 | FR-NM-02 | TestHardwareInfo | CPU count is positive int | Verify that get_cpu_count returns a positive integer. | None. | None. | 1. Call `get_cpu_count()`. | Returns int > 0. |
| TC-SI-07 | FR-NM-02 | TestHardwareInfo | Architecture is non-empty string | Verify that get_arch returns a non-empty string. | None. | None. | 1. Call `get_arch()`. | Returns non-empty string. |
| TC-SI-08 | FR-NM-02 | TestOSInfo | OS name is non-empty string | Verify that get_os_name returns a non-empty string. | None. | None. | 1. Call `get_os_name()`. | Returns non-empty string. |
| TC-SI-09 | FR-NM-02 | TestOSInfo | OS version is non-empty string | Verify that get_os_version returns a non-empty string. | None. | None. | 1. Call `get_os_version()`. | Returns non-empty string. |

### NodeServiceServicer (`tests/unit/test_heartbeat_service.py`)

Component under test: `orchestrator.service.NodeServiceServicer` - gRPC handler that bridges heartbeat and status requests to the NodeRegistry. Tests call servicer methods directly with mock context objects, no network I/O.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-HS-01 | FR-NM-01 | TestHeartbeatRPC | Heartbeat returns acknowledged | Verify that a valid heartbeat returns acknowledged=True. | Empty registry. | Default heartbeat request. | 1. Create servicer with empty registry. 2. Call `Heartbeat` with request. | response.acknowledged is True. |
| TC-HS-02 | FR-NM-02 | TestHeartbeatRPC | Heartbeat registers node | Verify that after a heartbeat the node exists in the registry with correct fields including inference_port. | Empty registry. | node_id="node-1", ip="192.168.1.10", available_ram=3800, total_ram=4096, cpu_count=4, arch="aarch64", inference_port=50052. | 1. Call `Heartbeat`. 2. Call `registry.get_node("node-1")`. | Node exists with all fields matching, status="available", inference_port=50052. |
| TC-HS-03 | FR-NM-01 | TestHeartbeatRPC | Heartbeat returns configured interval | Verify that the response carries the heartbeat interval configured on the servicer. | Empty registry, servicer with heartbeat_interval_ms=5000. | Default heartbeat request. | 1. Create servicer with interval=5000. 2. Call `Heartbeat`. | response.heartbeat_interval_ms == 5000. |
| TC-HS-04 | FR-NM-01 | TestHeartbeatRPC | Heartbeat default interval | Verify that the default heartbeat interval is 2000ms. | Empty registry. | Default heartbeat request. | 1. Create servicer with default config. 2. Call `Heartbeat`. | response.heartbeat_interval_ms == 2000. |
| TC-HS-05 | FR-NM-04 | TestReportStatusRPC | ReportStatus returns correct fields | Verify that ReportStatus returns all node fields for a known node. | Registry with one node registered via Heartbeat. | node_id="node-1". | 1. Send Heartbeat. 2. Call `ReportStatus(node_id="node-1")`. | All fields match, status=AVAILABLE. |
| TC-HS-06 | FR-NM-04 | TestReportStatusRPC | ReportStatus unknown node | Verify that ReportStatus sets NOT_FOUND for an unknown node. | Empty registry. | node_id="nonexistent". | 1. Call `ReportStatus(node_id="nonexistent")`. | context.set_code called with NOT_FOUND, context.set_details called. |

### HeartbeatClient (`tests/unit/test_heartbeat_client.py`)

Component under test: `worker.heartbeat.HeartbeatClient` - periodically sends gRPC heartbeats to the orchestrator from a background thread. Each test creates its own in-process gRPC server with a matching heartbeat interval so the server does not override the client's test interval.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-HC-01 | FR-NM-01 | TestHeartbeatClientLifecycle | Client sends heartbeat | Verify the client sends at least one heartbeat after starting. | In-process gRPC server with interval=200ms. | node_id="test-node", interval=0.2s, inference_port=50052. | 1. Start client. 2. Wait 0.5s. 3. Stop client. 4. Check registry. | Node exists with status="available". |
| TC-HC-02 | FR-NM-01 | TestHeartbeatClientLifecycle | Client stops cleanly | Verify that after stop(), is_running is False. | In-process gRPC server with interval=200ms. | node_id="test-node", interval=0.2s, inference_port=50052. | 1. Start client. 2. Assert is_running=True. 3. Stop client. | is_running is False. |
| TC-HC-03 | FR-NM-01 | TestHeartbeatClientLifecycle | Client sends multiple heartbeats | Verify heartbeats continue periodically, not just on startup. | In-process gRPC server with interval=200ms. | node_id="test-node", interval=0.2s, inference_port=50052. | 1. Start client. 2. Wait 1.0s. 3. Read last_heartbeat. 4. Compare to current time. | last_heartbeat is within 0.5s of current time. |
| TC-HC-06 | FR-NM-01 | TestHeartbeatClientLifecycle | Reports an explicit IP override | Verify passing `ip_address` pins the reported address instead of auto-detecting one - the escape hatch for a node where detection picks the wrong interface (e.g. Wi-Fi over Ethernet). | In-process gRPC server with interval=200ms. | ip_address="10.0.0.42". | 1. Start client with `ip_address="10.0.0.42"`. 2. Wait 0.5s. 3. Stop client. 4. Check registry. | Registered node's `ip_address` is "10.0.0.42". |
| TC-HC-04 | NFR-02 | TestHeartbeatClientResilience | Client handles server unavailable | Verify the client does not crash when the orchestrator is unreachable. | No server running on target port. | orchestrator_address="localhost:1", interval=0.2s. | 1. Start client. 2. Wait 0.5s. 3. Stop client. | is_running is False, no exceptions. |
| TC-HC-05 | NFR-02 | TestHeartbeatClientResilience | Client reconnects after orchestrator restarts | Verify the client resumes heartbeating on its own once a new orchestrator comes up on the same address, without needing to be restarted itself. | gRPC server started, then stopped; a second server started on the same port with a fresh registry. | node_id="test-node", interval=0.2s. | 1. Start client against the first server. 2. Stop the first server. 3. Start a second server on the same port. 4. Wait. | The second server's registry has the node registered. |

### Model Manifest (`tests/unit/test_manifest.py`)

Component under test: `inference.manifest` - YAML manifest parsing and validation. No torch dependency. Uses pytest `tmp_path` for temporary YAML files.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MF-01 | FR-MR-01 | TestLoadValidManifest | Load valid manifest | Verify a valid YAML produces a ModelManifest with all fields. | None. | Full valid manifest data. | 1. Write YAML to tmp file. 2. Call `load_manifest()`. | All 8 fields match input. |
| TC-MF-02 | FR-MR-01 | TestLoadValidManifest | Manifest is frozen | Verify ModelManifest is immutable. | None. | Valid manifest. | 1. Load manifest. 2. Try to assign a field. | Raises AttributeError. |
| TC-MF-03 | FR-MR-03 | TestMissingFields | Missing required field (parametrized x8) | Verify each missing required field raises ManifestError. | None. | Manifest with one field removed. | 1. Remove field. 2. Call `load_manifest()`. | Raises ManifestError matching field name. |
| TC-MF-04 | FR-MR-03 | TestInvalidValues | Unsupported arch | Verify unsupported architecture raises ManifestError. | None. | arch="rnn". | 1. Call `load_manifest()`. | Raises ManifestError matching "arch". |
| TC-MF-05 | FR-MR-03 | TestInvalidValues | Unsupported input type | Verify unsupported input type raises ManifestError. | None. | input_type="video". | 1. Call `load_manifest()`. | Raises ManifestError matching "input_type". |
| TC-MF-06 | FR-MR-03 | TestInvalidValues | Layers zero | Verify zero layers raises ManifestError. | None. | layers=0. | 1. Call `load_manifest()`. | Raises ManifestError matching "layers". |
| TC-MF-07 | FR-MR-03 | TestInvalidValues | Layers negative | Verify negative layers raises ManifestError. | None. | layers=-1. | 1. Call `load_manifest()`. | Raises ManifestError matching "layers". |
| TC-MF-08 | FR-MR-03 | TestInvalidValues | Name with spaces | Verify name with spaces raises ManifestError. | None. | name="my model". | 1. Call `load_manifest()`. | Raises ManifestError matching "name". |
| TC-MF-09 | FR-MR-03 | TestFileErrors | File not found | Verify nonexistent path raises ManifestError. | None. | path="/nonexistent/manifest.yaml". | 1. Call `load_manifest()`. | Raises ManifestError matching "not found". |
| TC-MF-10 | FR-MR-01 | TestManifestFromDict | Valid dict produces manifest | Verify a valid dict (not from a file) produces a ModelManifest with all fields. | None. | Full valid manifest data. | 1. Call `manifest_from_dict()` with the dict directly. | Returns ModelManifest with matching fields. |
| TC-MF-11 | FR-MR-03 | TestManifestFromDict | Invalid dict raises | Verify an invalid dict raises ManifestError, same as the file-loading path. | None. | arch="rnn". | 1. Call `manifest_from_dict()` with the dict directly. | Raises ManifestError matching "arch". |

### Model Registry (`tests/unit/test_model_registry.py`)

Component under test: `inference.model_registry.ModelRegistry` - thread-safe in-memory store of validated model manifests. No torch dependency.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MR-01 | FR-MR-01 | TestRegisterAndGet | Register and get | Verify a registered manifest can be retrieved by name. | Empty registry. | Valid manifest. | 1. Register manifest. 2. Call `get(name)`. | Returns manifest with matching fields. |
| TC-MR-02 | FR-MR-01 | TestRegisterAndGet | Duplicate raises | Verify registering the same name twice raises ValueError. | Registry with one model. | Same manifest. | 1. Register. 2. Register again. | Raises ValueError matching model name. |
| TC-MR-03 | FR-MR-01 | TestRegisterAndGet | Get unknown | Verify looking up an unregistered model returns None. | Empty registry. | name="nonexistent". | 1. Call `get("nonexistent")`. | Returns None. |
| TC-MR-04 | FR-MR-04 | TestListModels | List empty | Verify empty registry returns empty list. | Empty registry. | None. | 1. Call `list_models()`. | Returns []. |
| TC-MR-05 | FR-MR-04 | TestListModels | List all | Verify all registered models are returned. | Empty registry. | 3 models. | 1. Register 3 models. 2. Call `list_models()`. | Returns 3 items with correct names. |
| TC-MR-06 | FR-MR-05 | TestDeleteModel | Delete existing | Verify deleting an existing model returns True and removes it. | Registry with one model. | name="mamba-130m". | 1. Delete. 2. Call `get(name)`. | Returns True, get returns None. |
| TC-MR-07 | FR-MR-05 | TestDeleteModel | Delete nonexistent | Verify deleting a nonexistent model returns False. | Empty registry. | name="nonexistent". | 1. Call `delete("nonexistent")`. | Returns False. |
| TC-MR-08 | NFR-02 | TestThreadSafety | Concurrent register | Verify 20 threads registering different models causes no corruption. | Empty registry. | 20 unique model names. | 1. Spawn 20 threads. 2. Join. 3. Call `list_models()`. | No exceptions, 20 models in registry. |

### HTTP API (`tests/unit/test_http_api.py`)

Component under test: `orchestrator.http_api` - the FastAPI app that exposes the node and model registries over HTTP for the TUI and WebUI. Tests call the app directly through a `TestClient` fixture (`fastapi_test_client`), wired to real `NodeRegistry` and `ModelRegistry` instances. No gRPC or model loading involved. TestPostInferStreaming covers `POST /infer` with `stream=true`: an `application/x-ndjson` response (one line per generated token, then a final result line) built on `PipelineRunner.generate_stream()` instead of the buffered `generate()` path.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-HA-01 | FR-NM-04 | TestGetNodes | Empty registry returns empty list | Verify GET /nodes on an empty registry returns an empty list. | Empty NodeRegistry. | None. | 1. Call `GET /nodes`. | 200, body is []. |
| TC-HA-02 | FR-NM-04 | TestGetNodes | Populated registry returns nodes | Verify a registered node is returned with its fields over HTTP. | NodeRegistry with one node registered directly via `update_node`. | node_id="node-1", inference_port=50052. | 1. Register node-1. 2. Call `GET /nodes`. | 200, one item with node_id="node-1", status="available", inference_port=50052. |
| TC-HA-03 | FR-MR-04 | TestGetModels | Empty registry returns empty list | Verify GET /models on an empty registry returns an empty list. | Empty ModelRegistry. | None. | 1. Call `GET /models`. | 200, body is []. |
| TC-HA-04 | FR-MR-04 | TestGetModels | Populated registry returns models | Verify a registered model is returned with its fields over HTTP. | ModelRegistry with mamba-130m registered directly. | Full valid manifest. | 1. Register manifest. 2. Call `GET /models`. | 200, one item with name="mamba-130m", arch="mamba". |
| TC-HA-05 | FR-MR-02 | TestPostModels | Valid manifest registers and is retrievable | Verify POST /models with valid YAML registers the model and it appears in a subsequent GET /models. | Empty ModelRegistry. | Full valid manifest YAML. | 1. Call `POST /models`. 2. Call `GET /models`. | POST returns 201 with name="mamba-130m"; GET returns 1 item. |
| TC-HA-06 | FR-MR-03 | TestPostModels | Missing field returns 400 | Verify a manifest missing a required field returns 400 naming the field. | Empty ModelRegistry. | YAML missing "checkpoint" and other fields. | 1. Call `POST /models`. | 400, detail matches "checkpoint". |
| TC-HA-07 | FR-MR-03 | TestPostModels | Unsupported arch returns 400 | Verify an unsupported architecture returns 400 naming the field. | Empty ModelRegistry. | arch="rnn". | 1. Call `POST /models`. | 400, detail matches "arch". |
| TC-HA-08 | FR-MR-01 | TestPostModels | Duplicate name returns 409 | Verify registering the same model name twice returns 409 on the second call. | ModelRegistry with mamba-130m already registered via a prior POST. | Same manifest YAML twice. | 1. POST once. 2. POST again. | Second call returns 409. |
| TC-HA-09 | FR-MR-03 | TestPostModels | Invalid YAML returns 400 | Verify malformed YAML returns 400 rather than a server error. | Empty ModelRegistry. | manifest_yaml="not: valid: yaml: [structure". | 1. Call `POST /models`. | 400. |
| TC-HA-10 | FR-MR-03 | TestPostModels | Non-mapping YAML returns 400 | Verify YAML that parses but is not a mapping (e.g. a list) returns 400. | Empty ModelRegistry. | manifest_yaml="- one\n- two\n". | 1. Call `POST /models`. | 400. |
| TC-HA-11 | FR-IE-01 | TestPostInfer | Unknown model returns 404 | Verify POST /infer for an unregistered model name returns 404. | Empty ModelRegistry. | model_name="nonexistent". | 1. Call `POST /infer`. | 404. |
| TC-HA-12 | FR-DE-01 | TestPostInfer | No available nodes returns 503 | Verify POST /infer surfaces a DispatchError with no available nodes as 503, not a 500. | ModelRegistry with mamba-130m registered; `plan_dispatch` mocked to raise DispatchError("no available nodes in the registry"). | model_name="mamba-130m". | 1. Call `POST /infer`. | 503, detail matches "no available". |
| TC-HA-13 | FR-IE-05, FR-IE-06 | TestPostInfer | Success path returns output and latency | Verify a successful request returns generated text plus timing, per-node memory, and token-count fields. `_run_inference` now calls `runner.generate()` (cached autoregressive decoding) instead of looping `run_forward()` itself; timing/memory fields are read from the last entry of the returned `GenerationResult.step_results`. | ModelRegistry with mamba-130m registered; `plan_dispatch`, `ModelStore`, `PipelineRunner`, `AutoTokenizer.from_pretrained` mocked; one-node fake plan; fake `PipelineRunner.generate` returns a fake `GenerationResult` (output_ids, one step_result with node_latencies_ms=[5.0], node_peak_memory_mb=[260]). | max_new_tokens=1. | 1. Call `POST /infer`. | 200; output="hello world"; node_latencies_ms=[5.0]; peak_memory_mb=[260]; num_nodes=1; num_tokens=1; latency_ms >= 0; runner.load called once. |
| TC-HA-14 | FR-IE-04 | TestPostInfer | Second call reuses resident runner | Verify a second request for the same model reuses the loaded pipeline instead of dispatching and loading shards again. | Same mocks as TC-HA-13. | Two requests, same model_name. | 1. Call `POST /infer` twice. | PipelineRunner constructed once; plan_dispatch called once; runner.load called once; runner.generate called twice. |
| TC-HA-27 | FR-IE-04, FR-IE-05 | TestPostInferStreaming | stream=true returns ndjson media type | Verify a stream=true request gets an application/x-ndjson response instead of the plain JSON object the non-streaming path returns. | Same mocks as TC-HA-13, but PipelineRunner.generate_stream() yields real GenerationStep objects instead of generate() returning a GenerationResult. | max_new_tokens=2, stream=true. | 1. Call `POST /infer` with stream=true. | 200; content-type starts with "application/x-ndjson". |
| TC-HA-28 | FR-IE-04, FR-IE-05, FR-IE-06 | TestPostInferStreaming | Yields a chunk per token then a final result | Verify each generated token arrives as its own NDJSON line ({"token": ..., "done": false}), followed by exactly one final line ({"done": true, ...}) carrying the same metrics fields the non-streaming response returns in one shot. | Same streaming mocks as TC-HA-27, tokenizer.decode side_effect returns one string per token plus a final full-text decode. | next_tokens=(111, 222). | 1. Call `POST /infer` with stream=true. 2. Parse each NDJSON line. | 3 lines: two {"token": "tok111"/"tok222", "done": false}, then a final line with done=true, output="tok111 tok222", node_latencies_ms=[5.0], peak_memory_mb=[260], num_nodes=1, num_tokens=2. |
| TC-HA-29 | FR-IE-01 | TestPostInferStreaming | stream=true unknown model still returns 404 | Verify an unregistered model name returns a normal 404 even when streaming is requested - the model-existence check happens before the response commits to a streaming status. | Empty ModelRegistry. | model_name="nonexistent", stream=true. | 1. Call `POST /infer` with stream=true. | 404. |
| TC-HA-30 | FR-IE-04 | TestPostInferStreaming | stream=false or omitted does not call generate_stream | Verify omitting stream (or passing false) uses the existing runner.generate() path unchanged - generate_stream() is only invoked when a client actually asks for streaming. | Same mocks as TC-HA-13. | max_new_tokens=1, stream omitted. | 1. Call `POST /infer` without stream. | runner.generate_stream not called; runner.generate called once. |
| TC-HA-15 | FR-IE-01 | TestPostModelLoad | Unknown model returns 404 | Verify POST /models/{name}/load for an unregistered name returns 404. | Empty ModelRegistry. | name="nonexistent". | 1. Call `POST /models/nonexistent/load`. | 404. |
| TC-HA-16 | FR-IE-01, FR-DE-03 | TestPostModelLoad | Starts loading and reports ready | Verify a load request kicks off dispatch/load in the background and the model eventually reports ready. | ModelRegistry with mamba-130m registered; `plan_dispatch`, `ModelStore`, `PipelineRunner`, `AutoTokenizer.from_pretrained` mocked; one-node fake plan. | None. | 1. `POST /models/mamba-130m/load`. 2. Poll `GET /models/mamba-130m/status` until terminal. | POST returns immediately with status "loading"/"ready"; status eventually "ready" with num_nodes=1; runner.load called once. |
| TC-HA-17 | FR-DE-01 | TestPostModelLoad | No available nodes reports error | Verify a dispatch failure surfaces as an error status rather than an exception. | ModelRegistry with mamba-130m registered; `plan_dispatch` mocked to raise DispatchError("no available nodes in the registry"). | None. | 1. `POST /models/mamba-130m/load`. 2. Poll status until terminal. | status="error", error matches "no available". |
| TC-HA-18 | FR-IE-04 | TestPostModelLoad | Second call does not reload | Verify calling load twice while already loading/loaded only dispatches once. | Same mocks as TC-HA-16. | Two load calls. | 1. `POST /models/mamba-130m/load` twice, with a status check between. | plan_dispatch called once; runner.load called once. |
| TC-HA-19 | FR-IE-01 | TestGetModelStatus | Unknown model returns 404 | Verify GET /models/{name}/status for an unregistered name returns 404. | Empty ModelRegistry. | name="nonexistent". | 1. Call `GET /models/nonexistent/status`. | 404. |
| TC-HA-20 | FR-IE-01 | TestGetModelStatus | Never-loaded model reports not_loaded | Verify a registered but never-loaded model reports status "not_loaded". | ModelRegistry with mamba-130m registered, never loaded. | None. | 1. Call `GET /models/mamba-130m/status`. | 200, status="not_loaded". |
| TC-HA-21 | FR-DE-03 | TestGetTopology | No model loaded returns empty | Verify GET /topology with nothing loaded reports no model and no assignments. | Empty state. | None. | 1. Call `GET /topology`. | 200, `{model_name: null, assignments: []}`. |
| TC-HA-22 | FR-DE-03 | TestGetTopology | Reports assignments once dispatched | Verify topology reports the real per-node layer assignments once a model has dispatched, even mid-load. | Same mocks as TC-HA-16, one assignment (node-0, layers [0,24), is_first=is_last=True). | None. | 1. `POST /models/mamba-130m/load`. 2. Call `GET /topology`. | model_name="mamba-130m"; one assignment matching node-0's fields. |
| TC-HA-23 | FR-IE-01 | TestPostModelRedistribute | Unknown model returns 404 | Verify POST /models/{name}/redistribute for an unregistered name returns 404. | Empty ModelRegistry. | name="nonexistent". | 1. Call `POST /models/nonexistent/redistribute`. | 404. |
| TC-HA-24 | FR-DE-01 | TestPostModelRedistribute | Never-loaded model returns 400 | Verify redistributing a model that was never loaded is a 400, not a crash. | ModelRegistry with mamba-130m registered, never loaded. | None. | 1. Call `POST /models/mamba-130m/redistribute`. | 400. |
| TC-HA-25 | FR-DE-01, FR-DE-03 | TestPostModelRedistribute | Unloads old session and redispatches | Verify a ready model has its old shards unloaded and is dispatched again from scratch, picking up the current node registry. | Same mocks as TC-HA-16, model already loaded and ready. | None. | 1. `POST /models/mamba-130m/load`, wait for ready. 2. `POST /models/mamba-130m/redistribute`. 3. Wait for ready again. | 200; runner.unload called once; plan_dispatch called twice total; runner.load called twice total; final status "ready". |
| TC-HA-26 | FR-IE-04 | TestPostModelRedistribute | Still loading returns 409 | Verify redistributing while a load is already in progress is rejected rather than racing it. | Same mocks as TC-HA-16, runner.load blocked on an Event until the test releases it. | None. | 1. `POST /models/mamba-130m/load` (blocks in background). 2. `POST /models/mamba-130m/redistribute` while still loading. | 409. |

### Server (`tests/unit/test_server.py`)

Component under test: `orchestrator.server.register_existing_manifests` - scans a directory for `*.yaml` manifests and registers each one at startup, so existing manifests need no manual re-upload. Calls the real `ModelRegistry` and a temporary directory, no gRPC or HTTP server involved.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-SRV-01 | FR-MR-01 | TestRegisterExistingManifests | Registers every valid manifest in directory | Verify a valid *.yaml file in the directory is registered. | Empty ModelRegistry. | One valid manifest file, mamba-130m.yaml. | 1. Write manifest to tmp_path. 2. Call `register_existing_manifests`. | `model_registry.get("mamba-130m")` is not None. |
| TC-SRV-02 | FR-MR-03 | TestRegisterExistingManifests | Skips invalid manifest without raising | Verify an invalid manifest is logged and skipped rather than raised. | Empty ModelRegistry. | broken.yaml missing required fields. | 1. Write invalid manifest to tmp_path. 2. Call `register_existing_manifests`. | No exception raised; `model_registry.list_models()` is empty. |
| TC-SRV-03 | FR-MR-01 | TestRegisterExistingManifests | Missing directory does nothing | Verify a nonexistent manifests directory is a no-op, not an error. | Empty ModelRegistry. | Directory path that does not exist. | 1. Call `register_existing_manifests` with a nonexistent path. | No exception raised; `model_registry.list_models()` is empty. |
| TC-SRV-04 | FR-MR-01 | TestRegisterExistingManifests | Ignores non-YAML files | Verify non-.yaml files in the directory are not treated as manifests. | Empty ModelRegistry. | README.md containing plain text. | 1. Write README.md to tmp_path. 2. Call `register_existing_manifests`. | `model_registry.list_models()` is empty. |

### Tensor Serialization (`tests/unit/test_tensor_utils.py`)

Component under test: `inference.tensor_utils` - serializes PyTorch tensors to raw bytes with shape and dtype metadata for gRPC transport, and reconstructs them on the receiving side. Requires torch, no model download.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-TU-01 | FR-IE-04 | TestRoundtrip | Roundtrip float32 | Verify a float32 tensor survives serialize-deserialize with correct values. | None. | [1.0, 2.5, -3.7] float32. | 1. Serialize. 2. Deserialize. 3. Compare. | Values match, dtype is float32. |
| TC-TU-02 | FR-IE-04 | TestRoundtrip | Roundtrip int64 | Verify an int64 tensor (token IDs) survives roundtrip. | None. | [101, 2023, 3045, 0] int64. | 1. Serialize. 2. Deserialize. 3. Compare. | Values match, dtype is int64. |
| TC-TU-03 | FR-IE-04 | TestRoundtrip | Roundtrip 2D | Verify a 2D tensor shape (1, 128) is preserved. | None. | Random int64 (1, 128). | 1. Serialize. 2. Deserialize. 3. Check shape. | Shape is (1, 128), values match. |
| TC-TU-04 | FR-IE-04 | TestRoundtrip | Roundtrip 3D | Verify a 3D tensor shape (1, 128, 768) is preserved. | None. | Random float32 (1, 128, 768). | 1. Serialize. 2. Deserialize. 3. Check shape. | Shape is (1, 128, 768), values match. |
| TC-TU-05 | FR-IE-04 | TestErrors | Unknown dtype raises | Verify unsupported dtype string raises ValueError. | None. | dtype_str="torch.bfloat16". | 1. Call `deserialize_tensor` with unsupported dtype. | Raises ValueError matching "dtype". |
| TC-TU-06 | FR-IE-04 | TestErrors | Shape mismatch raises | Verify mismatched data size and shape raises ValueError. | None. | 3-element float32 data, shape=[10]. | 1. Serialize 3 floats. 2. Deserialize with shape [10]. | Raises ValueError. |

### Model Loader (`tests/unit/test_loader.py`)

Component under test: `inference.loader` - loads HuggingFace models and tokenizers, runs tokenization and generation. Unit tests mock the model class via `patch.dict(_ARCH_TO_MODEL_CLASS)` to avoid downloading models.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-ML-01 | FR-IE-01 | TestLoadModel | Unsupported arch raises | Verify loading an unsupported architecture raises NotImplementedError. | None. | arch="s4". | 1. Call `load_model` with arch="s4". | Raises NotImplementedError matching "s4". |
| TC-ML-02 | FR-IE-01 | TestLoadModel | Load returns handle | Verify loading a model returns a ModelHandle with all fields set. | Mocked model class and tokenizer. | Default manifest. | 1. Call `load_model`. 2. Check handle fields. | name, model, tokenizer, manifest, loaded_at all set. |
| TC-ML-03 | FR-IE-01 | TestLoadModel | Load sets eval mode | Verify the model is set to eval mode after loading. | Mocked model class. | Default manifest. | 1. Call `load_model`. 2. Check mock. | `model.eval()` called once. |
| TC-ML-04 | FR-IE-01 | TestLoadModel | Load sets CPU | Verify the model is moved to CPU after loading. | Mocked model class. | Default manifest. | 1. Call `load_model`. 2. Check mock. | `model.to("cpu")` called once. |
| TC-ML-05 | FR-IE-01 | TestUnloadModel | Unload clears references | Verify unloading sets model and tokenizer to None. | ModelHandle with mock model/tokenizer. | None. | 1. Call `unload_model`. 2. Check handle. | model is None, tokenizer is None. |

### InferenceServiceServicer (`tests/unit/test_inference_service.py`)

Component under test: `inference.service.InferenceServiceServicer` - gRPC handler that bridges LoadShard/RunShard/UnloadShard requests to the model loader. Unit tests mock the loader via `patch("inference.service.load_model")` to avoid model downloads. TestRunShardCache is the exception: it uses a real (tiny, random-weight) `MambaShardModule`-backed `ModelHandle` instead of a mock, to exercise the actual recurrent-state cache reset/reuse logic (`request.reset_cache`) in pipeline-mode RunShard.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-IS-01 | FR-IE-01 | TestLoadShard | Load shard success | Verify LoadShard with valid parameters returns success. | Mocked loader. | Default load request. | 1. Call `LoadShard`. | success=True, memory_used_mb=260, layers_loaded=24. |
| TC-IS-02 | FR-IE-01 | TestLoadShard | Load shard stores model | Verify the model is stored in the servicer's internal dict. | Mocked loader. | Default load request. | 1. Call `LoadShard`. 2. Check `_models`. | Model present in dict. |
| TC-IS-03 | FR-IE-01 | TestLoadShard | Load shard duplicate rejects | Verify loading the same name twice returns failure. | Mocked loader, one model loaded. | Same model name twice. | 1. Load. 2. Load again. | success=False, "already loaded". |
| TC-IS-04 | FR-IE-01 | TestLoadShard | Load shard failure | Verify loader exceptions produce success=False with error message. | Mocked loader raises. | arch="s4". | 1. Call `LoadShard`. | success=False, error contains "s4". |
| TC-IS-11 | FR-IE-01 | TestLoadShard | Load shard from bytes path | Verify LoadShard with non-empty shard_weights calls load_shard_from_bytes instead of load_model and reports layers_loaded as layer_end - layer_start. | Mocked load_shard_from_bytes. | shard_weights=b"fake", layer_start=0, layer_end=12. | 1. Call `LoadShard` with shard_weights set. | success=True, load_shard_from_bytes called once, layers_loaded=12. |
| TC-IS-05 | FR-IE-04 | TestRunShard | Run shard model not loaded | Verify RunShard for unloaded model sets NOT_FOUND. | Empty servicer. | model_name="mamba-130m". | 1. Call `RunShard`. | success=False, context.set_code(NOT_FOUND). |
| TC-IS-06 | FR-IE-04 | TestRunShard | Run shard generate mode | Verify RunShard in generate mode returns output tensor. | Mocked loader, model loaded, mock model.generate. | generate_mode=True. | 1. Load. 2. RunShard. | success=True, output_tensor non-empty, latency > 0. |
| TC-IS-07 | FR-IE-04 | TestRunShard | Run shard forward pass | Verify RunShard in forward-pass mode returns logits. | Mocked loader, model loaded, mock model(). | generate_mode=False. | 1. Load. 2. RunShard. | success=True, output_tensor non-empty. |
| TC-IS-08 | FR-IE-06 | TestRunShard | Run shard records latency | Verify RunShard records positive latency in response. | Mocked loader, model loaded. | Default run request. | 1. Load. 2. RunShard. | latency_ms > 0. |
| TC-IS-12 | FR-IE-04, FR-IE-05 | TestRunShard | Pipeline forwards to next worker | Verify RunShard in pipeline mode with next_worker_address set fires WorkerClient.run_shard and returns acknowledgement only. | Mocked load_shard_from_bytes and WorkerClient, model loaded with next_worker_address="192.168.1.11:50052". | orchestrator_callback_address="localhost:50060". | 1. LoadShard via bytes path. 2. RunShard with orchestrator_callback_address set. | success=True, WorkerClient called with next address, run_shard called once. |
| TC-IS-13 | FR-IE-04, FR-IE-05 | TestRunShard | Last shard delivers result | Verify RunShard in pipeline mode with empty next_worker_address calls PipelineCallbackClient.deliver_result. | Mocked load_shard_from_bytes and PipelineCallbackClient, model loaded with next_worker_address="". | orchestrator_callback_address="localhost:50060". | 1. LoadShard via bytes path. 2. RunShard with orchestrator_callback_address set. | success=True, PipelineCallbackClient called with callback address, deliver_result called once. |
| TC-IS-14 | FR-IE-04 | TestRunShardCache | reset_cache=True builds a fresh cache | Verify a pipeline-mode RunShard call with reset_cache=True builds a new cache on the handle, using a real (tiny, random-weight) single-shard ModelHandle rather than a mock, to exercise actual cache lifecycle behavior. | Real tiny single-shard ModelHandle (2 layers), handle.cache starts None. | reset_cache=True. | 1. Call RunShard with reset_cache=True. | response.success=True; handle.cache is not None. |
| TC-IS-15 | FR-IE-04 | TestRunShardCache | reset_cache=False reuses existing cache | Verify a subsequent pipeline-mode RunShard call with reset_cache=False reuses the same cache object instead of rebuilding it from scratch. | Same real tiny single-shard ModelHandle, cache already built by a prior reset_cache=True call. | First call reset_cache=True, second call reset_cache=False with a single new token. | 1. RunShard with reset_cache=True. 2. RunShard with reset_cache=False. | handle.cache is the same object identity after both calls. |
| TC-IS-16 | FR-IE-04 | TestRunShardCache | reset_cache=True again rebuilds cache | Verify a second reset_cache=True call (e.g. a fresh, unrelated generation against the same resident model) discards the old cache and builds a new one, so no state leaks between generations. | Same real tiny single-shard ModelHandle. | Two consecutive calls, both reset_cache=True. | 1. RunShard with reset_cache=True. 2. RunShard with reset_cache=True again. | handle.cache after the second call is not the same object as after the first. |
| TC-IS-17 | FR-IE-04 | TestRunShardCache | Cached decode matches full recompute | Verify a prefill (reset_cache=True) followed by a cached decode step (reset_cache=False), both driven through the real RunShard RPC handler, produce the same final-position logits as a single RunShard call recomputing the whole sequence with no cache reuse - the actual point of this feature, verified at the service layer. | Real tiny single-shard ModelHandle (2 layers, deterministic weights via fixed torch seed). | Random 3-token prompt plus one random next token (vocab=32). | 1. RunShard on full_sequence with reset_cache=True (reference). 2. RunShard on prompt with reset_cache=True, then on next_token with reset_cache=False (cached), on a fresh handle seeded identically. 3. Compare final-position logits from both DeliverResult payloads. | torch.allclose(reference_logits, cached_logits, atol=1e-4). |
| TC-IS-09 | FR-IE-01 | TestUnloadShard | Unload shard success | Verify UnloadShard removes model and returns memory freed. | Mocked loader, one model loaded. | model_name="mamba-130m". | 1. Load. 2. Unload. | success=True, model removed from dict. |
| TC-IS-10 | FR-IE-01 | TestUnloadShard | Unload shard not loaded | Verify UnloadShard for unknown model returns failure. | Empty servicer. | model_name="nonexistent". | 1. Call `UnloadShard`. | success=False. |

### Dispatch Engine (`tests/unit/test_dispatch.py`)

Component under test: `orchestrator.dispatch` - pure layer partitioning and shard assignment logic. No network or model I/O. Tests use a real `NodeRegistry` populated via `update_node` and a `MagicMock` manifest.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-DE-01 | FR-DE-02 | TestSplitLayers | Even split | Verify equal-sized ranges when total_layers divides evenly by num_nodes. | None. | total_layers=24, num_nodes=2. | 1. Call `split_layers(24, 2)`. | Returns [(0, 12), (12, 24)]. |
| TC-DE-02 | FR-DE-02 | TestSplitLayers | Uneven split, last takes remainder | Verify the last range absorbs leftover layers when division is not exact. | None. | total_layers=10, num_nodes=3. | 1. Call `split_layers(10, 3)`. | Returns [(0, 3), (3, 6), (6, 10)]. |
| TC-DE-03 | FR-DE-02 | TestSplitLayers | Single node gets full range | Verify a single node receives the complete [0, total_layers) range. | None. | total_layers=24, num_nodes=1. | 1. Call `split_layers(24, 1)`. | Returns [(0, 24)]. |
| TC-DE-04 | FR-DE-02 | TestSplitLayers | Raises when more nodes than layers | Verify DispatchError is raised when num_nodes exceeds total_layers. | None. | total_layers=2, num_nodes=3. | 1. Call `split_layers(2, 3)`. | Raises DispatchError matching "nodes". |
| TC-DE-05 | FR-DE-01, FR-DE-02 | TestPlanDispatch | Correct layer ranges | Verify each assignment receives the correct contiguous layer range from split_layers. | Registry with two available nodes. | layers=24, 2 nodes. | 1. Call `plan_dispatch`. 2. Extract (layer_start, layer_end) pairs. | Ranges are [(0, 12), (12, 24)]. |
| TC-DE-06 | FR-DE-02 | TestPlanDispatch | is_first and is_last flags | Verify the first assignment has is_first=True and the last has is_last=True; others have both False. | Registry with two available nodes. | layers=24, 2 nodes. | 1. Call `plan_dispatch`. 2. Check flags on first and last assignments. | assignments[0].is_first=True, is_last=False; assignments[-1].is_first=False, is_last=True. |
| TC-DE-07 | FR-DE-02 | TestPlanDispatch | next_worker_address points to next node | Verify a non-last assignment's next_worker_address is "ip:port" of the immediately following node. | Registry with two nodes, distinct ports. | node-0 at 192.168.1.10:50052, node-1 at 192.168.1.11:50053. | 1. Call `plan_dispatch`. 2. Check assignments[0].next_worker_address. | "192.168.1.11:50053". |
| TC-DE-08 | FR-DE-02 | TestPlanDispatch | Last assignment has empty next_worker_address | Verify the last assignment's next_worker_address is an empty string. | Registry with two available nodes. | layers=24, 2 nodes. | 1. Call `plan_dispatch`. 2. Check assignments[-1].next_worker_address. | "". |
| TC-DE-09 | FR-DE-01 | TestPlanDispatch | Raises with empty registry | Verify DispatchError is raised when no available nodes are in the registry. | Empty NodeRegistry. | layers=24. | 1. Call `plan_dispatch`. | Raises DispatchError matching "no available". |
| TC-DE-10 | FR-DE-01 | TestPlanDispatch | Uses all available nodes | Verify plan_dispatch produces exactly one assignment per available node. | Registry with three available nodes. | layers=24, 3 nodes. | 1. Call `plan_dispatch`. 2. Check len(assignments). | 3 assignments. |
| TC-DE-11 | FR-DE-02 | TestPlanDispatch | Single node is both first and last | Verify that with one node, is_first=True, is_last=True, and next_worker_address="". | Registry with one available node. | layers=24, 1 node. | 1. Call `plan_dispatch`. 2. Check flags and address. | is_first=True, is_last=True, next_worker_address="". |
| TC-DE-12 | FR-DE-02 | TestPlanDispatch | Middle node has no first or last flag | Verify that a middle assignment has is_first=False and is_last=False. | Registry with three available nodes. | layers=24, 3 nodes. | 1. Call `plan_dispatch`. 2. Check assignments[1]. | is_first=False, is_last=False. |
| TC-DE-13 | FR-DE-03 | TestPlanDispatch | Plan carries manifest metadata | Verify DispatchPlan stores arch, model_name, and total_layers from the manifest. | Registry with one available node. | arch="mamba", name="mamba-130m", layers=24. | 1. Call `plan_dispatch`. 2. Check plan fields. | arch="mamba", model_name="mamba-130m", total_layers=24. |

### Worker Client (`tests/unit/test_worker_client.py`)

Component under test: `orchestrator.worker_client.WorkerClient` and `PipelineCallbackClient` - thin gRPC stub wrappers for inter-node communication. Tests mock `grpc.insecure_channel` and the stub constructors to avoid real network connections.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-WC-01 | FR-IE-04 | TestWorkerClient | load_shard delegates to stub | Verify load_shard calls stub.LoadShard with the provided request. | Mocked channel and InferenceServiceStub. | LoadShardRequest(model_name="mamba-130m"). | 1. Create WorkerClient with mocked stub. 2. Call `load_shard(request)`. | stub.LoadShard called once with request. |
| TC-WC-02 | FR-IE-04 | TestWorkerClient | run_shard delegates to stub | Verify run_shard calls stub.RunShard with the provided request. | Mocked channel and InferenceServiceStub. | RunShardRequest(model_name="mamba-130m"). | 1. Create WorkerClient with mocked stub. 2. Call `run_shard(request)`. | stub.RunShard called once with request. |
| TC-WC-03 | FR-IE-04 | TestWorkerClient | unload_shard delegates to stub | Verify unload_shard calls stub.UnloadShard with the provided request. | Mocked channel and InferenceServiceStub. | UnloadShardRequest(model_name="mamba-130m"). | 1. Create WorkerClient with mocked stub. 2. Call `unload_shard(request)`. | stub.UnloadShard called once with request. |
| TC-WC-04 | FR-IE-04 | TestWorkerClient | Context manager closes channel | Verify that exiting the context manager closes the gRPC channel exactly once. | Mocked channel and InferenceServiceStub. | address="localhost:50052". | 1. Use WorkerClient as context manager. 2. Exit context. | channel.close() called once. |
| TC-WC-05 | FR-IE-04 | TestWorkerClient | Channel configured with large message size | Verify the channel is created with max_send and max_receive options of at least 512 MB. | None. | address="localhost:50052". | 1. Create WorkerClient. 2. Inspect options passed to grpc.insecure_channel. | grpc.max_send_message_length >= 512 MB and grpc.max_receive_message_length >= 512 MB. |
| TC-WC-06 | FR-IE-05 | TestPipelineCallbackClient | deliver_result delegates to stub | Verify deliver_result calls stub.DeliverResult with the provided request. | Mocked channel and PipelineCallbackServiceStub. | DeliverResultRequest(request_id="req-abc"). | 1. Create PipelineCallbackClient with mocked stub. 2. Call `deliver_result(request)`. | stub.DeliverResult called once with request. |

### Shard Module (`tests/unit/test_shard.py`)

Tests for `MambaShardModule` forward pass shapes using lightweight mock sub-modules (an `IdentityLayer` that accepts and ignores `cache_params`, matching the real `MambaBlock` signature). No real model weights required, except TestMambaShardModuleCache which uses a real tiny `MambaForCausalLM` to verify config retention and cache sizing.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-SM-01 | FR-IE-04 | TestMambaShardModuleForward | First shard forward shape | Verify the first shard embeds token IDs to hidden states. | None. | batch=1, seq=5, d_model=8, vocab=100, nn.Embedding + IdentityLayer. | 1. Create first shard. 2. Pass token IDs. | output.shape == (1, 5, 8). |
| TC-SM-02 | FR-IE-04 | TestMambaShardModuleForward | Middle shard forward shape | Verify a middle shard passes hidden states through unchanged. | None. | batch=1, seq=5, d_model=8, IdentityLayer. | 1. Create middle shard. 2. Pass hidden states. | output.shape == (1, 5, 8). |
| TC-SM-03 | FR-IE-04 | TestMambaShardModuleForward | Last shard forward shape | Verify the last shard projects hidden states to logits. | None. | batch=1, seq=5, d_model=8, vocab=100, IdentityLayer + LayerNorm + Linear. | 1. Create last shard. 2. Pass hidden states. | output.shape == (1, 5, 100). |
| TC-SM-04 | FR-IE-04 | TestMambaShardModuleForward | Single shard forward shape | Verify a shard with is_first=True and is_last=True runs the full pipeline (embed -> layer -> norm -> lm_head). | None. | batch=1, seq=5, d_model=8, vocab=100. | 1. Create single shard. 2. Pass token IDs. | output.shape == (1, 5, 100). |
| TC-SM-05 | FR-IE-04 | TestMambaShardModuleForward | Multiple layers applied in order | Verify each layer in the ModuleList is applied sequentially. | None. | Two ScaleBy2 layers; input all-ones. | 1. Create middle shard with two ScaleBy2 layers. 2. Forward pass. | All output values == 4.0. |
| TC-SM-06 | FR-IE-04 | TestMambaShardModuleValidation | Requires embeddings when is_first | Verify ValueError when is_first=True but embeddings is None. | None. | is_first=True, embeddings=None. | 1. Construct MambaShardModule. | Raises ValueError matching "embeddings". |
| TC-SM-07 | FR-IE-04 | TestMambaShardModuleValidation | Requires norm and lm_head when is_last | Verify ValueError when is_last=True but norm_f or lm_head is None. | None. | is_last=True, norm_f=None. | 1. Construct MambaShardModule. | Raises ValueError matching "norm_f". |
| TC-SM-08 | FR-IE-04 | TestMambaShardModuleCache | forward threads cache_params into each layer | Verify forward()'s cache_params argument is passed through to every layer's forward call, not silently dropped. | None. | A RecordingLayer that stores whatever cache_params it receives; a sentinel object. | 1. Create shard with RecordingLayer. 2. Call shard(x, cache_params=sentinel). | layer.received is sentinel. |
| TC-SM-09 | FR-IE-04 | TestMambaShardModuleCache | from_model retains full model config | Verify from_model() keeps a reference to the full model's config, even for a shard covering only a sub-range of its layers. | None. | Real tiny MambaConfig (4 layers); shard covering layers [2,4). | 1. Call MambaShardModule.from_model with layer_start=2, layer_end=4. | shard.config is model.config; shard.config.num_hidden_layers == 4. |
| TC-SM-10 | FR-IE-04 | TestMambaShardModuleCache | from_bytes retains full model config for sub-range shard | Verify from_bytes() retains the full model's config (not one sized to just this shard's own layer range) - the guard against the cache-sizing landmine where Cache.layers[layer_idx] is indexed by the model's original, global layer_idx. | None. | Real tiny MambaConfig (4 layers); source shard for layers [2,4), serialized via torch.save then reconstructed via from_bytes. | 1. Build and serialize a sub-range shard. 2. Reconstruct via from_bytes with the full config JSON. | shard.config.num_hidden_layers == 4; len(shard.layers) == 2. |
| TC-SM-11 | FR-IE-04 | TestMambaShardModuleCache | new_cache sized from full config not shard layer count | Verify new_cache() sizes the cache from the full model's layer count (self.config.num_hidden_layers), never len(self.layers) - the exact landmine this feature must avoid reintroducing. | None. | Real tiny MambaConfig (4 layers); shard covering layers [2,4) (2 layers). | 1. Build sub-range shard via from_model. 2. Call shard.new_cache(). | len(cache.layers) == 4; len(cache.layers) != len(shard.layers). |

### Shard Cache Regression (`tests/unit/test_shard_cache.py`)

Component under test: the recurrent-state caching mechanism end to end at the shard layer - `MambaShardModule.new_cache()` plus `transformers.cache_utils.DynamicCache` threaded through two pipeline shards. Builds a tiny in-memory `MambaForCausalLM` (random weights, no checkpoint on disk) so these stay fast, self-contained unit tests with no file I/O dependency - only internal consistency between the cached and non-cached paths matters, not the (meaningless) numerical values themselves.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-SC-01 | FR-IE-04 | TestNewCacheSizing | new_cache has one slot per full model layer | Verify a cache built from a shard covering only part of the model still allocates one slot per layer of the *full* model, not just this shard's own layers - the config-sizing landmine guard, exercised through the real new_cache() API. | None. | Real tiny MambaConfig (4 layers); shard covering layers [2,4). | 1. Build sub-range shard via from_model. 2. Call shard.new_cache(). | len(cache.layers) == 4 (total_layers); len(shard.layers) == 2. |
| TC-SC-02 | FR-IE-04 | TestCachedDecodeParity | Cached decode matches full recompute | Verify a prefill + one cached decode step across two real pipeline shards produces the same final-position logits as a single full-recompute pass with no cache. | None. | Two shards splitting a tiny 4-layer MambaForCausalLM at the midpoint; random prompt (1,3) plus one random next token. | 1. Run full_sequence through both shards with no cache (reference). 2. Run prompt then next_token through both shards, each with its own new_cache(). 3. Compare final-position logits. | torch.allclose(full_last_logits, cached_last_logits, atol=1e-4). |

### Pipeline Coordinator (`tests/unit/test_pipeline.py`)

Tests for `ResultStore`, `PipelineCallbackServicer`, and `PipelineRunner`, including `generate()`'s cached autoregressive decoding and its `_generation_lock`. All `WorkerClient` calls are mocked; no real network I/O occurs.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-PC-01 | FR-IE-05 | TestResultStore | wait returns delivered result | Verify that delivering a result to a created slot unblocks wait and returns the PipelineResult. | None. | PipelineResult with output tensor and latency lists. | 1. create_slot. 2. deliver from background thread. 3. wait. | Returns the exact PipelineResult delivered. |
| TC-PC-02 | FR-IE-05 | TestResultStore | wait raises on timeout | Verify wait raises TimeoutError when no result is delivered within the timeout. | None. | timeout_s=0.01. | 1. create_slot. 2. wait without delivering. | Raises TimeoutError matching the request_id. |
| TC-PC-03 | FR-IE-05 | TestPipelineCallbackServicer | DeliverResult resolves slot | Verify that calling DeliverResult on the servicer deserializes the tensor and delivers to ResultStore. | Slot created in store. | output shape (1, 3, 50280), latencies [8.5, 11.2], memory [240, 310]. | 1. create_slot. 2. Call DeliverResult. 3. wait. | acknowledged=True; result has correct tensor shape, latencies, memory. |
| TC-PC-04 | FR-IE-04, FR-IE-05 | TestPipelineRunnerLoad | load sends LoadShard to each worker | Verify load() calls WorkerClient.load_shard once per assignment. | Mocked ModelStore and WorkerClient. | Two-node plan. | 1. Call load(). | load_shard.call_count == 2. |
| TC-PC-05 | FR-IE-04, FR-IE-05 | TestPipelineRunnerLoad | load sends correct next_worker_address to first worker | Verify the first LoadShard request carries the second worker's "ip:port". | Mocked ModelStore and WorkerClient. | Two-node plan: node-0 next_worker="192.168.1.11:50052". | 1. Call load(). 2. Inspect first load_shard call_args. | first_request.next_worker_address == "192.168.1.11:50052". |
| TC-PC-06 | FR-IE-04, FR-IE-05 | TestPipelineRunnerLoad | load sends empty next_worker_address to last worker | Verify the last LoadShard request has next_worker_address="". | Mocked ModelStore and WorkerClient. | Two-node plan. | 1. Call load(). 2. Inspect last load_shard call_args. | last_request.next_worker_address == "". |
| TC-PC-07 | FR-IE-04, FR-IE-05 | TestPipelineRunnerRunForward | run_forward sends RunShard only to first worker | Verify WorkerClient is created exactly once, targeting the first worker's address. | Mocked WorkerClient whose run_shard delivers a result. | Two-node plan, first node at "192.168.1.10:50052". | 1. Call run_forward. | WorkerClient called once with "192.168.1.10:50052". |
| TC-PC-08 | FR-IE-04, FR-IE-05 | TestPipelineRunnerRunForward | run_forward includes request_id and callback_address | Verify the RunShard request carries a non-empty UUID and the orchestrator callback address. | Mocked WorkerClient delivering a result. | callback_address="localhost:50060". | 1. Call run_forward. 2. Inspect run_shard call_args. | request_id != "", orchestrator_callback_address == "localhost:50060". |
| TC-PC-09 | FR-IE-04, FR-IE-05 | TestPipelineRunnerRunForward | run_forward returns PipelineResult after future resolves | Verify run_forward returns the correct PipelineResult once the slot is delivered. | Mocked WorkerClient whose run_shard side_effect delivers to the store. | expected_tensor shape (1, 3, 50280), latencies [10.0, 12.0]. | 1. Call run_forward. | Returns PipelineResult with matching tensor and latencies. |
| TC-PC-10 | FR-IE-04, FR-IE-05 | TestPipelineRunnerRunForward | run_forward raises on timeout | Verify run_forward raises TimeoutError when no DeliverResult arrives before timeout_s. | Mocked WorkerClient that does NOT deliver a result. | timeout_s=0.01. | 1. Call run_forward. | Raises TimeoutError. |
| TC-PC-11 | FR-IE-04, FR-IE-05 | TestPipelineRunnerUnload | unload sends UnloadShard to all workers | Verify unload() calls WorkerClient.unload_shard once per assignment. | Mocked WorkerClient. | Two-node plan. | 1. Call unload(). | unload_shard.call_count == 2. |
| TC-PC-12 | FR-IE-04, FR-IE-05 | TestPipelineRunnerUnload | Unload continues past an unreachable worker | Verify a dead node (e.g. crashed before a redistribute) doesn't stop the remaining nodes from being unloaded. | Mocked WorkerClient; unload_shard raises UNAVAILABLE on the first call, succeeds on the second. | Two-node plan. | 1. Call unload(). | No exception raised; unload_shard.call_count == 2. |
| TC-PC-13 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerate | First step resets cache and sends full prompt | Verify generate()'s first RunShardRequest has reset_cache=True and carries the entire prompt, not just the newest token. | Mocked WorkerClient whose run_shard side_effect delivers an argmax-crafted result per call. | prompt=[[1,2,3]], max_new_tokens=3. | 1. Call generate(prompt, 3). 2. Inspect the first run_shard call's request. | requests[0].reset_cache is True; deserialized input_tensor equals the full prompt. |
| TC-PC-14 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerate | Subsequent steps reuse cache and send only newest token | Verify steps after the first have reset_cache=False and carry only the single newest generated token, not the growing sequence. | Same mocked WorkerClient as TC-PC-13. | prompt=[[1,2,3]], max_new_tokens=3, next_tokens=[111,222,333]. | 1. Call generate(prompt, 3). 2. Inspect requests[1] and requests[2]. | Each has reset_cache=False; deserialized input_tensor has shape (1,1) equal to the previous step's generated token. |
| TC-PC-15 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerate | Returns output_ids and one step_result per token | Verify generate() returns a GenerationResult whose output_ids concatenates the prompt with every generated token, and whose step_results has exactly one PipelineResult per generated token. | Mocked WorkerClient delivering tokens [111, 222]. | prompt=[[1,2,3]], max_new_tokens=2. | 1. Call generate(prompt, 2). | result.output_ids == [[1,2,3,111,222]]; len(result.step_results) == 2, all PipelineResult instances. |
| TC-PC-16 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerate | Concurrent generate calls are serialized | Verify two overlapping generate() calls against the same runner never run concurrently - the _generation_lock must hold for the entire call, since interleaving would corrupt worker-side cache state. | Mocked WorkerClient whose run_shard side_effect tracks concurrent-active count and sleeps 0.05s per call. | Two threads, each calling generate(prompt, 2) on the same runner. | 1. Start both threads. 2. Join both. | max_active concurrent run_shard executions == 1. |
| TC-PC-17 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerateStream | Yields one step per token | Verify generate_stream() (the generator form of generate(), used by the streaming /infer endpoint) yields exactly max_new_tokens GenerationStep objects, each carrying a token_id and the PipelineResult that produced it. | Mocked WorkerClient whose run_shard side_effect delivers an argmax-crafted result per call. | prompt=[[1,2,3]], max_new_tokens=3, next_tokens=[111,222,333]. | 1. Call list(runner.generate_stream(prompt, 3)). | len(steps) == 3; all GenerationStep instances; token_id values equal next_tokens in order; all step_result values are PipelineResult instances. |
| TC-PC-18 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerateStream | First step resets cache, subsequent do not | Verify generate_stream() drives the same reset_cache protocol as generate(): reset_cache=True with the full prompt on the first step, reset_cache=False with just the newest token afterward. | Mocked WorkerClient whose run_shard side_effect delivers an argmax-crafted result per call. | prompt=[[1,2,3]], max_new_tokens=2, next_tokens=[111,222]. | 1. Call list(runner.generate_stream(prompt, 2)). 2. Inspect requests[0] and requests[1]. | requests[0].reset_cache is True; requests[1].reset_cache is False. |
| TC-PC-19 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerateStream | Lock held for entire iteration, not just first step | Verify the _generation_lock stays held for as long as the caller keeps iterating the generator, not just until the first token is yielded - an overlapping generate_stream()/generate() call against the same runner must still be serialized. | Mocked WorkerClient whose run_shard side_effect tracks concurrent-active count and sleeps 0.05s per call. | Two threads, each fully consuming runner.generate_stream(prompt, 2) via list(). | 1. Start both threads. 2. Join both. | max_active concurrent run_shard executions == 1. |
| TC-PC-20 | FR-IE-04, FR-IE-05 | TestPipelineRunnerGenerateStream | generate() matches generate_stream() output | Verify generate() (refactored to consume generate_stream() internally rather than duplicating its logic) still returns the same output_ids/step_results shape as before the refactor - a regression guard for the internal-sharing change. | Mocked WorkerClient delivering tokens [111, 222]. | prompt=[[1,2,3]], max_new_tokens=2. | 1. Call generate(prompt, 2). | result.output_ids == [[1,2,3,111,222]]; len(result.step_results) == 2. |

## Integration Tests

### Heartbeat Flow (`tests/integration/test_heartbeat_flow.py`)

Tests the full gRPC roundtrip - real server on localhost, real channel, real protobuf serialization. Validates that heartbeats sent over the wire reach the registry correctly.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-HF-01 | FR-NM-01 | TestHeartbeatRoundtrip | Heartbeat acknowledged over gRPC | Verify a heartbeat sent through a real gRPC channel returns acknowledged. | In-process gRPC server running. | Default heartbeat request. | 1. Create stub from channel. 2. Call `stub.Heartbeat()`. | acknowledged=True, heartbeat_interval_ms=2000. |
| TC-HF-02 | FR-NM-02 | TestHeartbeatRoundtrip | Multiple heartbeats same node | Verify multiple heartbeats from the same node produce one registry entry. | In-process gRPC server running. | 3 heartbeats with node_id="node-1". | 1. Send 3 heartbeats with same node_id. 2. Call `registry.list_nodes()`. | 1 node in registry. |
| TC-HF-03 | FR-NM-02 | TestHeartbeatRoundtrip | Two different nodes | Verify heartbeats from two nodes create two registry entries. | In-process gRPC server running. | node-1 and node-2 with different IPs. | 1. Send heartbeat for node-1. 2. Send heartbeat for node-2. 3. Call `registry.list_nodes()`. | 2 nodes, IDs {"node-1", "node-2"}. |
| TC-HF-04 | FR-NM-02 | TestHeartbeatRoundtrip | Heartbeat data reaches registry | Verify all fields survive gRPC serialization and reach the registry, including inference_port. | In-process gRPC server running. | Full heartbeat request with all fields set, inference_port=50052. | 1. Send heartbeat. 2. Call `registry.get_node("node-1")`. | All fields match request, status="available", inference_port=50052. |
| TC-HF-05 | FR-NM-04 | TestReportStatusRoundtrip | ReportStatus known node over gRPC | Verify ReportStatus returns correct fields over a real gRPC channel. | In-process gRPC server, one node registered. | node_id="node-1". | 1. Send heartbeat. 2. Call `stub.ReportStatus()`. | Fields match, status=AVAILABLE. |
| TC-HF-06 | FR-NM-04 | TestReportStatusRoundtrip | ReportStatus unknown node raises | Verify ReportStatus raises gRPC NOT_FOUND for unknown nodes. | In-process gRPC server, empty registry. | node_id="nonexistent". | 1. Call `stub.ReportStatus()`. | Raises RpcError with code NOT_FOUND. |

### Failure Detection and End-to-End (`tests/integration/test_failure_detection.py`)

Tests the full orchestrator lifecycle: gRPC server + reaper thread + HeartbeatClients, all in-process. Short intervals (0.1s heartbeat, 0.3s timeout, 0.05s reaper) keep tests fast.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-FD-05 | FR-NM-03 | TestFailureDetection | Node drops after missed heartbeats | Verify a node that stops heartbeating is marked unavailable by the reaper. | Full orchestrator running (server + reaper). | node_id="node-1", interval=0.1s, timeout=0.3s. | 1. Start orchestrator. 2. Start client. 3. Wait 0.3s, verify available. 4. Stop client. 5. Wait for timeout. | Node status is "unavailable". |
| TC-FD-06 | FR-NM-05 | TestFailureDetection | Node rejoins after restart | Verify a previously unavailable node is restored when it resumes heartbeating. | Full orchestrator running, one node previously reaped. | node_id="node-1", interval=0.1s. | 1. Start client, wait, stop. 2. Wait for reap. 3. Start new client with same node_id. 4. Wait 0.3s. | Node status is "available". |
| TC-E2E-01 | FR-NM-01, FR-NM-02, FR-NM-03 | TestEndToEnd | Two workers, one dies | Sprint acceptance test: two workers register, one is killed, the dead one is reaped, the live one stays available. | Full orchestrator running. | worker-1 and worker-2, interval=0.1s. | 1. Start both clients. 2. Wait 0.3s, verify 2 available. 3. Stop worker-2. 4. Wait for timeout. | worker-1 available, worker-2 unavailable. |

### Model Loading (`tests/integration/test_model_loading.py`)

Tests the full HuggingFace model loading and inference pipeline with the real Mamba-130M model. Marked `@pytest.mark.slow`. Module-scoped fixture loads the model once, shared across tests.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MLI-01 | FR-IE-01 | TestModelLoading | Model handle fields | Verify the loaded handle has correct name, non-None model/tokenizer, and non-negative memory. | Model loaded via fixture. | mamba-130m manifest. | 1. Check handle fields. | name="mamba-130m", model/tokenizer not None, memory_mb >= 0. |
| TC-MLI-02 | FR-IE-02 | TestEndToEndInference | Tokenize returns tensors | Verify tokenize returns input_ids and attention_mask as 2D int64 tensors. | Model loaded via fixture. | prompt="Hey how are you doing?". | 1. Call `tokenize()`. 2. Check types and shapes. | Both 2D, same shape, int64. |
| TC-MLI-03 | FR-IE-01 | TestEndToEndInference | Generate returns string | Verify generation produces a non-empty string. | Model loaded via fixture. | Same prompt, max_new_tokens=10. | 1. Tokenize. 2. Generate. | Non-empty string. |
| TC-MLI-04 | FR-IE-01 | TestEndToEndInference | Reference output match | Verify deterministic output matches the known reference. | Model loaded via fixture. | Same prompt, max_new_tokens=30. | 1. Tokenize. 2. Generate. 3. Compare. | Exact match with reference string. |
| TC-MLI-05 | NFR-04 | TestEndToEndInference | No warnings on stderr | Verify no warning messages appear on stderr during inference. | None (runs in subprocess). | Same prompt. | 1. Run inference in subprocess. 2. Filter stderr for warnings. | No warning lines (progress bars allowed). |
| TC-MLI-06 | FR-IE-04 | TestShardRoundtrip | Two-shard roundtrip matches full model | Verify that extracting two shards, serializing each to bytes, reconstructing via from_bytes, and running in sequence produces logits matching a full model forward pass within atol=1e-5. | Model loaded via fixture. | mamba-130m, split at layer 12, input_ids=[101, 2023, 3045]. | 1. Extract shard0 (layers 0-11, is_first=True) and shard1 (layers 12-23, is_last=True). 2. Serialize and reconstruct each via from_bytes. 3. Run full model forward. 4. Run pipeline forward (shard0 -> shard1). 5. Compare. | pipeline_logits shape matches, allclose(pipeline_logits, full_logits, atol=1e-5). |

### Inference Flow (`tests/integration/test_inference_flow.py`)

Tests the full inference gRPC roundtrip with real Mamba-130M model. `TestInferenceRoundtrip` is marked `@pytest.mark.slow`; its module-scoped fixture starts an in-process gRPC server with InferenceServiceServicer. `TestCachedGenerationRoundtrip` is the exception: it uses the tiny, local, random-weight `dummy-mamba-tiny` checkpoint over a *real two-node* gRPC pipeline (two `InferenceServiceServicer`-backed workers plus a real `PipelineCallbackServicer`, built directly via `ModelStore`/`DispatchPlan`/`PipelineRunner`, not through the HTTP layer), so it needs no download and stays in the fast suite.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-IF-01 | FR-IE-01, FR-IE-04 | TestInferenceRoundtrip | Load and run | Load model via gRPC, run inference, verify non-empty output. | In-process gRPC server. | mamba-130m, token IDs, generate_mode=True. | 1. LoadShard. 2. RunShard. | Both succeed, output_tensor non-empty, latency > 0. |
| TC-IF-02 | FR-IE-04 | TestInferenceRoundtrip | Run without load | RunShard before LoadShard raises NOT_FOUND. | In-process gRPC server, no model loaded. | model_name="nonexistent-model". | 1. RunShard. | Raises RpcError with NOT_FOUND. |
| TC-IF-03 | FR-IE-01 | TestInferenceRoundtrip | Unload after load | UnloadShard removes the model from the servicer. | In-process gRPC server, model loaded. | model_name="unload-test-model". | 1. LoadShard. 2. UnloadShard. | success=True, model gone from servicer. |
| TC-IF-04 | FR-IE-04, FR-IE-05, FR-IE-06 | TestCachedGenerationRoundtrip | Cached generate matches full recompute per step | Verify runner.generate()'s cached per-token logits match a from-scratch full-recompute reference at every step, over a real two-node gRPC pipeline (real WorkerClient calls, real reset_cache wire field, real ResultStore/DeliverResult) - not just the in-process shard/service-layer parity already covered by unit tests. | Two real InferenceServiceServicer-backed gRPC workers plus a real PipelineCallbackServicer, dummy-mamba-tiny loaded and dispatched across both (layers [0,2) and [2,4)). | 3-token random prompt (vocab=50277), max_new_tokens=3. | 1. runner.generate(prompt, 3) (cached). 2. For each step, runner.run_forward() on the growing sequence with reset_cache=True (full-recompute reference). 3. Compare final-position logits at each step. | Cached and reference logits match within atol=1e-4 at every step; cached.output_ids equals the reference's argmax-derived token sequence. |

### HTTP Inference Flow (`tests/integration/test_http_flow.py`)

Tests the full inference flow through the HTTP layer with real workers - two in-process gRPC servers each hosting a real `InferenceServiceServicer`, and a third hosting `PipelineCallbackServicer`, all reached via the orchestrator's real FastAPI app (`create_app`) through a `TestClient`. Uses the tiny random-weight `dummy-mamba-tiny` checkpoint (`scripts/make_dummy_manifest.py`) rather than a full-size model - loading a real model here previously made this test slow enough, and heavy enough on RAM, that it was marked `@pytest.mark.slow`; it no longer needs that marker and now runs as part of the regular fast suite (`make test`). `make test-slow` remains for tests that do need a real model (`test_inference_flow.py`, `test_model_loading.py`).

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-HIF-01 | FR-IE-01, FR-IE-04, FR-IE-05, FR-IE-06 | TestHttpInferenceFlow | Infer over two-node pipeline | Verify POST /models followed by POST /infer produces non-empty output with per-node timing for both real workers. | Two real InferenceServiceServicer-backed gRPC servers registered as available nodes; a real PipelineCallbackServicer-backed gRPC server as the callback address. | Full dummy-mamba-tiny manifest YAML; prompt="Hey how are you doing?"; max_new_tokens=2. | 1. `POST /models`. 2. `POST /infer`. | Models call returns 201; infer call returns 200 with non-empty string output, num_nodes=2, node_latencies_ms has 2 entries. |

---

## Dashboard (Frontend) Tests

Component and utility tests for the `dashboard/` WebUI (Next.js, React 19). Run via `npm test` (Vitest + React Testing Library, jsdom environment). Tests live under `dashboard/tests/`, separate from the component/lib source they cover. Dashboard, Models, Inference, and Topology are all wired to the real HTTP API (`dashboard/lib/api.ts`); their tests mock that module with `vi.spyOn`.

### App Shell (`dashboard/tests/app/page.test.tsx`)

Component under test: `app/page.tsx` (the `Home` component) - the tab-switching shell around Dashboard/Models/Inference/Topology. All four pages mount once and stay mounted for the rest of the session; switching tabs toggles a `hidden` CSS class rather than conditionally rendering (`{currentPage === 'x' && <X/>}`), so a page's component state (chat history, selected model, load status, poll timers) survives tab switches instead of being torn down and rebuilt every time. Before this change, revisiting the Inference tab re-ran its mount effect on every visit, which combined with model auto-selection caused a model to load repeatedly just from switching tabs.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-APP-01 | FR-WUI-04 | Home (tab switching) | Keeps pages mounted across tab switches, preserving in-progress input | Verify that navigating away from Inference and back does not remount it - the selected model persists, and no extra `listModels()` call happens on the return visit. | `listNodes` resolves []; `listModels` resolves one manifest; `getTopology` resolves empty. | Full dummy-mamba-tiny manifest. | 1. Render `<Home />`. 2. Click "Inference", select the model. 3. Note `listModels` call count. 4. Click "Dashboard". 5. Click "Inference" again. | The model `<select>` still shows "dummy-mamba-tiny" after returning; `listModels` call count is unchanged from step 3 (no remount-triggered re-fetch). |

### Utils (`dashboard/tests/lib/utils.test.ts`)

Component under test: `dashboard/lib/utils.cn` - the `clsx` + `tailwind-merge` class-name helper used throughout the UI components.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-UT-01 | FR-WUI-01 | cn | Joins plain class names | Verify plain string class names are joined with a space. | None. | `cn('a', 'b')`. | 1. Call `cn('a', 'b')`. | Returns `"a b"`. |
| TC-UT-02 | FR-WUI-01 | cn | Drops falsy values | Verify falsy arguments are omitted from the result. | None. | `cn('a', false, undefined, null, 'b')`. | 1. Call `cn(...)`. | Returns `"a b"`. |
| TC-UT-03 | FR-WUI-01 | cn | Merges conflicting Tailwind classes | Verify a later conflicting Tailwind utility class wins over an earlier one. | None. | `cn('p-2', 'p-4')`. | 1. Call `cn('p-2', 'p-4')`. | Returns `"p-4"`. |

### Inference History (`dashboard/tests/lib/history.test.ts`)

Component under test: `dashboard/lib/history.ts` - a `localStorage`-backed log of completed inference requests (timestamp, model, latency, node count), appended to by the Inference page and read by the Dashboard's latency chart. `localStorage` is cleared after each test.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-IH-01 | FR-WUI-06 | getInferenceLog | Empty when nothing logged | Verify an empty `localStorage` returns an empty array rather than throwing. | Cleared `localStorage`. | None. | 1. Call `getInferenceLog()`. | Returns `[]`. |
| TC-IH-02 | FR-WUI-06 | appendInferenceLog | Persists an entry | Verify an appended entry is returned by a subsequent `getInferenceLog()` call. | Cleared `localStorage`. | One entry, latencyMs=120. | 1. Call `appendInferenceLog(entry)`. 2. Call `getInferenceLog()`. | Returns `[entry]`. |
| TC-IH-03 | FR-WUI-06 | appendInferenceLog | Appends rather than overwriting | Verify a second call adds to, rather than replaces, the log. | Cleared `localStorage`. | Two entries. | 1. Call `appendInferenceLog()` twice. 2. Call `getInferenceLog()`. | Returns 2 entries. |
| TC-IH-04 | FR-WUI-06 | appendInferenceLog | Caps at 50 entries, oldest first | Verify the log never grows unbounded - the oldest entries are dropped once it exceeds 50. | Cleared `localStorage`. | 55 entries, timestamps 0-54. | 1. Call `appendInferenceLog()` 55 times. 2. Call `getInferenceLog()`. | Returns 50 entries; oldest is timestamp=5, newest is timestamp=54. |
| TC-IH-05 | FR-WUI-06 | appendInferenceLog | Persists numTokens when provided | Verify the optional numTokens field (added for the tokens/sec chart) round-trips through localStorage - entries logged before this field existed simply won't have it. | Cleared `localStorage`. | One entry, numTokens=20. | 1. Call `appendInferenceLog(entry)` with numTokens set. 2. Call `getInferenceLog()`. | Returns `[entry]` including numTokens=20. |

### API Client (`dashboard/tests/lib/api.test.ts`)

Component under test: `dashboard/lib/api.ts` - the thin `fetch`-based wrapper around the orchestrator's HTTP API. Global `fetch` is mocked via `vi.stubGlobal`, no real network I/O.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-AC-01 | FR-WUI-02 | listNodes | Calls GET /nodes | Verify `listNodes()` calls `fetch` with the correct path and returns the parsed body. | `fetch` mocked to resolve 200 with a node array. | `[{ node_id: 'node-1' }]`. | 1. Call `listNodes()`. | `fetch` called with `/nodes`; returns the mocked array. |
| TC-AC-02 | FR-WUI-03 | listModels | Calls GET /models | Verify `listModels()` calls `fetch` with the correct path and returns the parsed body. | `fetch` mocked to resolve 200 with a model array. | `[{ name: 'dummy-mamba-tiny' }]`. | 1. Call `listModels()`. | `fetch` called with `/models`; returns the mocked array. |
| TC-AC-03 | FR-WUI-03 | submitModel | POSTs manifest YAML | Verify `submitModel()` POSTs the YAML wrapped as `{manifest_yaml}` and returns the registered model. | `fetch` mocked to resolve 201. | `manifestYaml="name: dummy-mamba-tiny\n"`. | 1. Call `submitModel(manifestYaml)`. | `fetch` called with `POST /models`, body `{"manifest_yaml": ...}`; returns the mocked model. |
| TC-AC-04 | FR-WUI-03 | submitModel | Throws backend detail on 400 | Verify a 400 response's `detail` message becomes the thrown error's message. | `fetch` mocked to resolve 400 with `{detail: "Missing required field: checkpoint"}`. | Invalid manifest YAML. | 1. Call `submitModel(...)`. | Rejects with an error matching "Missing required field: checkpoint". |
| TC-AC-05 | FR-WUI-03 | submitModel | Throws backend detail on 409 | Verify a 409 response's `detail` message becomes the thrown error's message. | `fetch` mocked to resolve 409 with a duplicate-name detail. | Duplicate manifest YAML. | 1. Call `submitModel(...)`. | Rejects with an error matching "already registered". |
| TC-AC-06 | FR-WUI-04 | runInference | POSTs model_name and input | Verify `runInference()` POSTs the expected body and returns the parsed `InferResult`. | `fetch` mocked to resolve 200 with a full inference result. | `modelName="dummy-mamba-tiny"`, `input="hi"`. | 1. Call `runInference(modelName, input)`. | `fetch` called with `POST /infer`, correct body; result has `output`/`num_nodes`. |
| TC-AC-07 | FR-WUI-04 | runInference | Includes max_new_tokens when provided | Verify the optional `maxNewTokens` argument is included in the request body only when given. | `fetch` mocked to resolve 200. | `maxNewTokens=5`. | 1. Call `runInference('dummy-mamba-tiny', 'hi', 5)`. | `fetch` body includes `"max_new_tokens": 5`. |
| TC-AC-08 | FR-WUI-04 | runInference | Throws backend detail on 404 | Verify a 404 response's `detail` message becomes the thrown error's message. | `fetch` mocked to resolve 404 with a not-found detail. | `modelName="nonexistent"`. | 1. Call `runInference('nonexistent', 'hi')`. | Rejects with an error matching "not found". |
| TC-AC-09 | FR-WUI-04 | loadModel | POSTs to /models/{name}/load | Verify `loadModel()` POSTs to the correct path and returns the parsed status. | `fetch` mocked to resolve 200 with a loading status. | `modelName="dummy-mamba-tiny"`. | 1. Call `loadModel(modelName)`. | `fetch` called with `POST /models/dummy-mamba-tiny/load`; returns `{status: 'loading', ...}`. |
| TC-AC-10 | FR-WUI-04 | loadModel | URL-encodes the model name | Verify a model name with special characters is URL-encoded in the path. | `fetch` mocked to resolve 200. | `modelName="weird name/x"`. | 1. Call `loadModel(modelName)`. | `fetch` called with `/models/weird%20name%2Fx/load`. |
| TC-AC-11 | FR-WUI-04 | getModelStatus | Calls GET /models/{name}/status | Verify `getModelStatus()` calls the correct path and returns the parsed body. | `fetch` mocked to resolve 200 with a ready status. | `modelName="dummy-mamba-tiny"`. | 1. Call `getModelStatus(modelName)`. | `fetch` called with `/models/dummy-mamba-tiny/status`; returns `{status: 'ready', num_nodes: 2}`. |
| TC-AC-12 | FR-WUI-04 | getModelStatus | Throws backend detail on 404 | Verify a 404 response's `detail` message becomes the thrown error's message. | `fetch` mocked to resolve 404 with a not-found detail. | `modelName="nonexistent"`. | 1. Call `getModelStatus('nonexistent')`. | Rejects with an error matching "not found". |
| TC-AC-13 | FR-WUI-05 | getTopology | Calls GET /topology | Verify `getTopology()` calls the correct path and returns the parsed body. | `fetch` mocked to resolve 200 with a model name and one assignment. | One assignment: node-0, layers [0,24). | 1. Call `getTopology()`. | `fetch` called with `/topology`; result has `model_name` and one assignment. |
| TC-AC-14 | FR-WUI-04 | redistributeModel | POSTs to /models/{name}/redistribute | Verify `redistributeModel()` POSTs to the correct path and returns the parsed status. | `fetch` mocked to resolve 200 with a loading status. | `modelName="dummy-mamba-tiny"`. | 1. Call `redistributeModel(modelName)`. | `fetch` called with `POST /models/dummy-mamba-tiny/redistribute`; returns `{status: 'loading', ...}`. |
| TC-AC-15 | FR-WUI-04 | runInferenceStream | Calls onToken per chunk, resolves with the final result | Verify `runInferenceStream()` POSTs with stream=true, reads the NDJSON response body via a ReadableStream reader, invokes the onToken callback for each token line, and resolves with the final `{done: true, ...}` line parsed as an `InferResult`. | `fetch` mocked with a `ReadableStream` body yielding NDJSON lines (2 token chunks + 1 final result). | modelName="dummy-mamba-tiny", input="hi". | 1. Call `runInferenceStream(modelName, input, onToken)`. | `fetch` called with `POST /infer`, body includes `stream: true`; onToken called with each token in order; result.output/num_tokens match the final line. |
| TC-AC-16 | FR-WUI-04 | runInferenceStream | Includes max_new_tokens when provided | Verify the optional `maxNewTokens` argument is included in the request body only when given, same as runInference(). | `fetch` mocked with a single-line NDJSON stream (final result only). | `maxNewTokens=5`. | 1. Call `runInferenceStream('dummy-mamba-tiny', 'hi', () => {}, 5)`. | `fetch` body includes `"stream": true` and `"max_new_tokens": 5`. |
| TC-AC-17 | FR-WUI-04 | runInferenceStream | Rejects on a mid-generation error | Verify a final NDJSON line carrying `{done: true, error: ...}` (rather than a full result) rejects the returned promise with that error message. | `fetch` mocked with a stream whose only line is `{done: true, error: "worker crashed"}`. | None. | 1. Call `runInferenceStream(...)`. | Rejects with an error matching "worker crashed". |
| TC-AC-18 | FR-WUI-04 | runInferenceStream | Throws backend detail on 404 | Verify a non-2xx response (e.g. unknown model) still throws the backend's detail message before any stream reading is attempted, same as runInference(). | `fetch` mocked to resolve 404 with a not-found detail (no body stream). | `modelName="nonexistent"`. | 1. Call `runInferenceStream('nonexistent', 'hi', () => {})`. | Rejects with an error matching "not found". |

### Sidebar (`dashboard/tests/components/sidebar.test.tsx`)

Component under test: `components/sidebar.tsx`, wired to `listNodes()` (mocked via `vi.spyOn`). The node count/status line previously showed a hardcoded "6 nodes • Active" left over from the original prototype; it now polls the real registry like every other page.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-SB-01 | FR-WUI-02 | Sidebar | Shows the real registered node count | Verify the sidebar reflects the actual number of registered nodes rather than a hardcoded figure. | `listNodes` mocked to resolve 2 nodes, both "available". | Two nodes. | 1. Render `<Sidebar />`. | "2 nodes • Active" present in the document. |
| TC-SB-02 | FR-WUI-02 | Sidebar | Shows Idle when no nodes are available | Verify an empty registry reads as idle rather than falsely claiming "Active". | `listNodes` mocked to resolve []. | None. | 1. Render `<Sidebar />`. | "0 nodes • Idle" present in the document. |

### Dashboard (`dashboard/tests/components/dashboard.test.tsx`)

Component under test: `components/pages/dashboard.tsx`, wired to `listNodes()` and `getInferenceLog()`. `listNodes` is mocked via `vi.spyOn(api, 'listNodes')`; the inference log is read straight from `localStorage` (cleared after each test), matching how the real Inference page writes to it via `appendInferenceLog()`. Node status shows *used* RAM (total minus available), not available RAM, to avoid reading as "this Pi is using 3.1GB" when 3.1GB was actually free. Metrics render through `MetricsPanel` (`components/metrics-panel.tsx`, see its own section below), which replaced the single always-visible `LatencyChart` with a filterable, tabbed Latency / Tokens-per-sec view built on the shared `MetricChart` component.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-DB-01 | FR-WUI-02 | Dashboard | Renders node status from listNodes() | Verify the dashboard page renders a real node returned by `listNodes()`. | `listNodes` mocked to resolve one node. | node_id="node-1", status="available", available_ram_mb=2048, total_ram_mb=4096. | 1. Render `<Dashboard />`. 2. Wait for async update. | "Total Nodes" label and "node-1" both present in the document. |
| TC-DB-02 | FR-WUI-02 | Dashboard | Shows an error message when listNodes() fails | Verify a fetch failure surfaces as a visible error rather than crashing. | `listNodes` mocked to reject. | Error("orchestrator unreachable"). | 1. Render `<Dashboard />`. 2. Wait for async update. | Error message text present in the document. |
| TC-DB-03 | FR-WUI-06 | Dashboard | Shows a placeholder when no prompts have been sent yet | Verify an empty inference log surfaces a clear placeholder rather than a blank chart. | Empty `localStorage`. | None. | 1. Render `<Dashboard />`. | "No inference requests sent yet..." message present in the document. |
| TC-DB-04 | FR-WUI-02, FR-WUI-06 | Dashboard | Plots latencies from the inference history log | Verify the metrics panel section reflects the real logged entry count. | `localStorage` pre-seeded with 2 entries. | latencyMs=120 and 340. | 1. Render `<Dashboard />`. | "Inference Metrics (2 prompts)" heading present in the document. |

### Metric Chart (`dashboard/tests/components/metric-chart.test.tsx`)

Component under test: `components/metric-chart.tsx`, a `recharts`-based single-series line chart used by both `MetricsPanel` tabs (Latency and Tokens/sec) - replaces an earlier hand-rolled SVG version that used a fixed `viewBox` + `preserveAspectRatio="none"`, which stretched non-uniformly (circles rendered as ellipses) whenever its container wasn't proportioned like the hardcoded viewBox. Takes plain `MetricPoint[]` (timestamp/value/modelName/numNodes) rather than history-log entries directly, so it has no opinion on which metric it's plotting. Renders a dot on every data point via `recharts`'s `Line dot` prop (not just on hover or the last point), compact x-axis timestamp labels that `recharts` auto-wraps onto two lines rather than rotating when they don't fit, and a linear/log y-axis scale toggle - log mode floors the axis to the smallest positive value's decade rather than 0, since log(0) is undefined. `jsdom` always reports 0x0 for `getBoundingClientRect()`, so `recharts`'s `ResponsiveContainer` renders nothing by default (the same class of limitation already documented for `@xyflow/react` in `topology.test.tsx`) - tests stub a real size on `Element.prototype.getBoundingClientRect` (scoped to this file only, restored in `afterEach`) so the chart actually renders its `<svg>` content instead of a blank container.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MC-01 | FR-WUI-06 | MetricChart | Shows the empty message when there are no points | Verify an empty points array renders the caller-provided placeholder instead of a blank chart. | None. | `points=[]`, `emptyMessage="Nothing here yet"`. | 1. Render `<MetricChart points={[]} .../>`. | "Nothing here yet" present in the document. |
| TC-MC-02 | FR-WUI-06 | MetricChart | Renders one line dot per data point | Verify every point gets its own visible marker, not just endpoints/hover. | `getBoundingClientRect` stubbed to 800x400. | 3 points. | 1. Render with 3 points. 2. Count `.recharts-line-dot` elements. | Exactly 3 dots rendered. |
| TC-MC-03 | FR-WUI-06 | MetricChart | Toggles the active scale button between linear and log | Verify clicking "log" moves the active-button styling from "linear" to "log" (and back is symmetric). | `getBoundingClientRect` stubbed to 800x400. | 3 points. | 1. Render chart. 2. Check initial button classes. 3. Click "log". 4. Recheck classes. | "linear" button starts with `bg-primary`, "log" doesn't; after clicking "log", the classes swap. |
| TC-MC-04 | FR-WUI-06 | MetricChart | Renders compact x-axis timestamp labels with no rotation/tilt | Verify the x-axis tick labels render non-empty text and no tick `<text>` carries a `rotate(...)` transform. | `getBoundingClientRect` stubbed to 800x400. | 3 points. | 1. Render chart. 2. Read `.recharts-xAxis-tick-labels` text content and check for a rotate transform. | Tick text is non-empty; no `text[transform*="rotate"]` found under `.recharts-xAxis-tick-labels`. |

### Metrics Panel (`dashboard/tests/components/metrics-panel.test.tsx`)

Component under test: `components/metrics-panel.tsx`, which wraps `MetricChart` with a filter toolbar (model, node count, date range - all client-side over the already-loaded `InferenceLogEntry[]`, no new API calls) and a Latency / Tokens-per-sec tab switcher. The Tokens/sec tab derives `numTokens / (latencyMs / 1000)` per entry and silently excludes entries that predate the `numTokens` field (logged before this feature existed) rather than showing a broken value for them. Since this renders `MetricChart` internally, its tests need the same `getBoundingClientRect` stub as that component's own tests (see above) for `recharts` to render anything in `jsdom`.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MP-01 | FR-WUI-06 | MetricsPanel | Shows the total prompt count with no filters applied | Verify the heading reflects every entry when no filter has been touched. | `getBoundingClientRect` stubbed to 800x400. | 3 entries (2x dummy-mamba-tiny, 1x dummy-mamba-small). | 1. Render `<MetricsPanel entries={ENTRIES} />`. | "Inference Metrics (3 prompts)" present in the document. |
| TC-MP-02 | FR-WUI-06 | MetricsPanel | Filters by model | Verify selecting a specific model in the filter dropdown narrows the plotted/counted entries to that model only. | `getBoundingClientRect` stubbed to 800x400. | Same 3 entries; 2 match "dummy-mamba-tiny". | 1. Render. 2. Change "Filter by model" to "dummy-mamba-tiny". | "Inference Metrics (2 prompts)" present in the document. |
| TC-MP-03 | FR-WUI-06 | MetricsPanel | Filters by node count | Verify selecting a specific node count in the filter dropdown narrows to entries with that exact numNodes. | `getBoundingClientRect` stubbed to 800x400. | Same 3 entries; 1 has numNodes=1. | 1. Render. 2. Change "Filter by node count" to "1". | "Inference Metrics (1 prompt)" present in the document. |
| TC-MP-04 | FR-WUI-06 | MetricsPanel | Switching to the Tokens/sec tab only plots entries with token counts | Verify the Tokens/sec tab computes tok/s from numTokens/latencyMs and correctly excludes the one entry missing numTokens, rather than plotting a broken/NaN point for it. | `getBoundingClientRect` stubbed to 800x400. | 3 entries; only 2 have numTokens set. | 1. Render. 2. Click the "Tokens/sec" tab. 3. Count `.recharts-line-dot` elements. | Exactly 2 dots rendered (the entry missing numTokens is excluded). |

### Models (`dashboard/tests/components/models.test.tsx`)

Component under test: `components/pages/models.tsx`, wired to `listModels()` and `submitModel()`. `listModels` is mocked via `vi.spyOn(api, 'listModels')`.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-MD-01 | FR-WUI-03 | Models | Renders the model list from listModels() | Verify the models page renders a real manifest returned by `listModels()`. | `listModels` mocked to resolve one manifest. | Full dummy-mamba-tiny manifest. | 1. Render `<Models />`. 2. Wait for async update. | "Models" heading and "dummy-mamba-tiny" entry present in the document. |
| TC-MD-02 | FR-WUI-03 | Models | Shows an error message when listModels() fails | Verify a fetch failure surfaces as a visible error rather than crashing. | `listModels` mocked to reject. | Error("orchestrator unreachable"). | 1. Render `<Models />`. 2. Wait for async update. | Error message text present in the document. |

### Inference (`dashboard/tests/components/inference.test.tsx`)

Component under test: `components/pages/inference.tsx`, wired to `listModels()`, `loadModel()`, `redistributeModel()`, `getModelStatus()`, and `runInferenceStream()`. All five are mocked via `vi.spyOn`. The model dropdown deliberately does NOT auto-select (and therefore does not auto-load) the first model once `listModels()` resolves - it shows a "Select a model..." placeholder, and `loadModel()` only fires once the user actively picks one via the dropdown (`<select id="model-select">`, associated with its label so `getByLabelText('Model')` works). Selecting a model triggers an immediate `loadModel()` call followed by polling `getModelStatus()`; the prompt input and send button stay disabled until the model reports "ready". Sending a prompt adds an empty assistant placeholder bubble immediately, then appends each streamed token to it as `runInferenceStream()`'s `onToken` callback fires - the placeholder shows a small spinner ("Generating...") only while it's still empty, so there's no separate floating "Processing..." indicator competing with the growing text; the transcript container also auto-scrolls to the bottom on every message/token update via a ref-based `scrollTop = scrollHeight` effect (not covered by an automated test - `jsdom` doesn't compute real layout, so `scrollHeight` is always 0 there). The Redistribute button re-dispatches the current model across whatever nodes are available now, via the same load-and-poll flow but calling `redistributeModel()` instead. Message bubbles wrap long text (`break-words whitespace-pre-wrap`, `overflow-x-hidden` on the transcript) instead of overflowing horizontally. The prompt input regains focus once a request completes (it's disabled mid-request, which drops focus), and every completed request is appended to the `localStorage`-backed inference log the Dashboard's metrics panel reads.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-INF-06 | FR-WUI-04 | Inference | Does not load any model just from listing them - loading is explicit | Verify that once `listModels()` resolves and populates the dropdown, `loadModel()` is NOT called, and the dropdown sits on the "Select a model..." placeholder (value="") rather than auto-selecting the first entry. | `listModels` resolves one manifest; `loadModel` mocked (should stay uncalled). | Full dummy-mamba-tiny manifest. | 1. Render `<Inference />`. 2. Wait for the model option to appear. 3. Wait ~50ms for any errant auto-load effect. | `loadModel` never called; the model `<select>` has value "". |
| TC-INF-01 | FR-WUI-04 | Inference | Shows a loading message while the model preloads, disabling the input | Verify explicitly selecting a model from the dropdown starts preloading it, and the prompt input stays disabled with a progress message until it is ready. | `listModels` resolves one manifest; `loadModel`/`getModelStatus` both resolve status="loading". | Full dummy-mamba-tiny manifest, num_nodes=1. | 1. Render `<Inference />`. 2. Wait for the model to appear in the dropdown. 3. Select it via `userEvent.selectOptions`. | "Loading model onto 1 node..." message shown; prompt input disabled with placeholder "Waiting for model to load...". |
| TC-INF-02 | FR-WUI-04, FR-WUI-06 | Inference | Enables the send button once ready, streams tokens as they arrive, refocuses the input, logs latency, and shows tokens/sec | Verify the prompt input enables once a selected model is ready; sending a prompt calls `runInferenceStream` with an onToken callback and renders each token as it's appended to the assistant bubble (not just the final buffered text); the input regains focus once sending finishes; the completed request is appended to the inference history log with numTokens; and the tokens/sec metric tile reflects `num_tokens / (latency_ms / 1000)`. | `loadModel`/`getModelStatus` resolve status="ready"; `runInferenceStream`'s mock implementation calls onToken twice then resolves with the final result. | prompt="hi there", tokens=["hello ", "world"], output="hello world", latency_ms=120, num_tokens=20. | 1. Render `<Inference />`. 2. Select the model from the dropdown. 3. Wait for input to enable. 4. Type prompt. 5. Click send button (queried by accessible name "Send"). 6. Wait for the input to re-enable. | `runInferenceStream` called with `('dummy-mamba-tiny', 'hi there', expect.any(Function))`; "hello world" rendered in the transcript; input is enabled and has focus; `getInferenceLog()` returns one entry matching the request including numTokens=20; "166.7 tok/s" present (20 tokens / 0.12s). |
| TC-INF-05 | FR-WUI-04 | Inference | Clicking Redistribute re-dispatches across the current nodes | Verify the Redistribute button calls `redistributeModel()` (not `loadModel()` again) once a selected model is ready. | `loadModel`/`getModelStatus` resolve status="ready"; `redistributeModel` resolves status="ready" with num_nodes=2. | None. | 1. Render `<Inference />`. 2. Select the model from the dropdown. 3. Wait for the Redistribute button to enable. 4. Click it. | `redistributeModel` called with `'dummy-mamba-tiny'`; `loadModel` still only called once (from the initial selection). |
| TC-INF-03 | FR-WUI-04 | Inference | Shows an error message when the model fails to load | Verify a dispatch failure while loading a selected model surfaces as a visible error instead of silently leaving the UI disabled. | `loadModel`/`getModelStatus` resolve status="error" with a "no available nodes" message. | error="no available nodes in the registry". | 1. Render `<Inference />`. 2. Select the model from the dropdown. | "Failed to load model: no available nodes..." message present in the document. |
| TC-INF-04 | FR-WUI-04 | Inference | Shows a message when no models are registered | Verify an empty model registry surfaces a clear message instead of a silently-disabled, unexplained UI. | `listModels` resolves []. | None. | 1. Render `<Inference />`. | "No models registered. Upload a manifest on the Models page." present in the document. |

### Topology (`dashboard/tests/components/topology.test.tsx`)

Component under test: `components/pages/topology.tsx`, wired to `listNodes()` and `getTopology()`, both mocked via `vi.spyOn`. Renders the real dispatch plan as a Blender-node-editor-style `@xyflow/react` graph (colored input/output sockets, dot-grid background) when a model is loaded, falling back to plain registered-node boxes when nothing has been dispatched yet. Every registered node is always rendered, including ones not part of the current pipeline (shown as idle) and the orchestrator itself. Node positions are drag-and-drop movable (`onNodesChange` wired to `useNodesState`) and persisted to `localStorage` so a dragged arrangement survives unmounting - which happens on every tab switch, since the page is conditionally rendered rather than routed. The "dispatch" and "result" edge labels use a custom `CallbackEdge` component with a fixed vertical offset in opposite directions, since for a single-node plan both edges connect the same orchestrator/node pair and would otherwise render on top of each other. This offset is not covered by an automated test: `@xyflow/react` only renders an edge once its connected nodes report real measured dimensions via `ResizeObserver`, and jsdom's `getBoundingClientRect()` always returns zero-size rects, so edges (and therefore edge labels) never render at all under this test harness - true of every test in this file, not just this one, which is why none of them assert on edge content.

| Test Case ID | Requirement | Test Suite | Title | Description | Pre-conditions | Test Data | Test Steps | Expected Result |
|---|---|---|---|---|---|---|---|---|
| TC-TP-01 | FR-WUI-05 | Topology | Shows registered nodes with no pipeline dispatched yet | Verify that with no model loaded, the page falls back to showing the raw registered cluster nodes rather than an empty or stale graph. | `listNodes` resolves one node; `getTopology` resolves `{model_name: null, assignments: []}`. | node_id="node-0". | 1. Render `<Topology />`. | "Network Topology" heading, "No model loaded yet - showing registered cluster nodes.", "node-0", and "Orchestrator" all present. |
| TC-TP-02 | FR-WUI-05 | Topology | Shows the real pipeline once a model has dispatched | Verify the graph reflects the actual per-node layer assignments and first/last roles returned by `GET /topology`, not mock data. | `listNodes` resolves one node; `getTopology` resolves one assignment (node-0, layers [0,4), is_first=is_last=True). | model_name="dummy-mamba-tiny". | 1. Render `<Topology />`. | "Showing the active pipeline for \"dummy-mamba-tiny\"." message shown; "layers [0, 4)", "first + last" role label, and "Orchestrator" all present. |
| TC-TP-04 | FR-WUI-05 | Topology | Shows a newly joined node as idle even while a pipeline is active | Verify a node that isn't part of the current dispatch plan still renders (as idle) instead of disappearing from the graph, and the idle count is called out in the status line. | `listNodes` resolves two nodes (node-0, node-1); `getTopology` resolves one assignment for node-0 only. | node-1 not in assignments. | 1. Render `<Topology />`. | "...1 node idle..." message shown; both "node-0" and "node-1" present (in the graph and the Status list). |
| TC-TP-05 | FR-WUI-05 | Topology | Restores a previously saved node position after remounting | Verify a node position saved to `localStorage` (simulating a prior drag, then a tab switch that unmounts and remounts the page) is used instead of the default layout position. | `localStorage` pre-seeded with `{'node-0': {x: 777, y: 888}}` before rendering. | node_id="node-0". | 1. Render `<Topology />`. 2. Wait for "node-0" to render. 3. Inspect the rendered node element's transform style. | The node's `transform` style includes "777px", not its default computed position. |
| TC-TP-03 | FR-WUI-05 | Topology | Shows an error message when the orchestrator is unreachable | Verify a `listNodes()` failure surfaces as a visible error rather than crashing or showing a blank graph. | `listNodes` mocked to reject. | Error("orchestrator unreachable"). | 1. Render `<Topology />`. | "orchestrator unreachable" message present in the document. |
